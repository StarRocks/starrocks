---
phase: 06-pr-review-resolution-test-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - be/test/exec/adbc_smoke_test.cpp
  - be/test/exec/ADBC_SMOKE_TEST_README.md
  - be/test/exec/CMakeLists.txt
  - .gitignore
  - thirdparty/build-thirdparty.sh
  - bin/start_backend.sh
  - be/src/exec/adbc_scanner.cpp
  - be/src/exec/adbc_scanner.h
  - be/src/connector/adbc_connector.cpp
  - be/src/exec/adbc_driver_registry.h
autonomous: true
requirements:
  - GOAL-06
user_setup: []

must_haves:
  truths:
    - "BE smoke test files do not exist on disk"
    - "CMakeLists.txt does not reference adbc_smoke_test"
    - ".gitignore does not contain .planning/"
    - "build-thirdparty.sh rocksdb and flatbuffers flags match main branch"
    - "BE ADBC source files contain zero GSD requirement tags"
  artifacts:
    - path: "be/test/exec/CMakeLists.txt"
      provides: "CMake config without smoke test target"
      contains: "No adbc_smoke_test references"
    - path: ".gitignore"
      provides: "Clean gitignore without .planning/ entry"
    - path: "thirdparty/build-thirdparty.sh"
      provides: "Build script without local workaround flags"
  key_links:
    - from: "be/test/exec/CMakeLists.txt"
      to: "be/test/exec/"
      via: "CMake target removal"
      pattern: "No adbc_smoke_test target"
---

<objective>
Delete throwaway BE smoke tests, revert local build workarounds, clean .gitignore, investigate start_backend.sh, and remove all GSD requirement tags from BE ADBC source comments.

Purpose: Resolve PR #1 review comments related to BE cleanup, build system, and config files. These are all independent deletions/reverts that do not affect FE code.
Output: Clean BE test directory, reverted build flags, no GSD references in BE ADBC code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pr-review-resolution-test-hardening/06-CONTEXT.md
@.planning/phases/06-pr-review-resolution-test-hardening/06-PATTERNS.md
@.planning/phases/06-pr-review-resolution-test-hardening/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete BE smoke tests and CMakeLists entries</name>
  <files>be/test/exec/adbc_smoke_test.cpp, be/test/exec/ADBC_SMOKE_TEST_README.md, be/test/exec/CMakeLists.txt</files>
  <read_first>
    - be/test/exec/CMakeLists.txt (current state, identify exact lines 46-54 to remove)
    - be/test/exec/adbc_smoke_test.cpp (confirm file exists before deleting)
    - be/test/exec/ADBC_SMOKE_TEST_README.md (confirm file exists before deleting)
  </read_first>
  <action>
1. Delete the file `be/test/exec/adbc_smoke_test.cpp` entirely.
2. Delete the file `be/test/exec/ADBC_SMOKE_TEST_README.md` entirely.
3. Edit `be/test/exec/CMakeLists.txt` to remove the ADBC smoke test target block (lines 46-54). The block to remove starts with `# ADBC smoke tests` and includes:
   - `add_library(starrocks_adbc_smoke_test_objs OBJECT ...)`
   - `target_link_libraries(starrocks_adbc_smoke_test_objs PRIVATE StarRocksPCH)`
   - `set_target_properties(starrocks_adbc_smoke_test_objs ...)`
   - `add_executable(adbc_smoke_test ...)`
   - `target_link_libraries(adbc_smoke_test ...)`
   Remove these lines entirely. Do NOT touch any other targets in the file.
  </action>
  <verify>
    <automated>test ! -f be/test/exec/adbc_smoke_test.cpp && test ! -f be/test/exec/ADBC_SMOKE_TEST_README.md && ! grep -q "adbc_smoke_test" be/test/exec/CMakeLists.txt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `be/test/exec/adbc_smoke_test.cpp` does not exist
    - `be/test/exec/ADBC_SMOKE_TEST_README.md` does not exist
    - `be/test/exec/CMakeLists.txt` contains zero occurrences of `adbc_smoke_test`
    - All other CMake targets in `be/test/exec/CMakeLists.txt` remain intact (file still has `add_executable` entries for other tests)
  </acceptance_criteria>
  <done>BE smoke test files deleted, CMakeLists cleaned. No other CMake targets affected.</done>
</task>

<task type="auto">
  <name>Task 2: Revert build workarounds, clean .gitignore, check start_backend.sh</name>
  <files>.gitignore, thirdparty/build-thirdparty.sh, bin/start_backend.sh</files>
  <read_first>
    - .gitignore (full file, identify lines 126-127 with `.planning/` entry)
    - thirdparty/build-thirdparty.sh (lines 775-785 for rocksdb flags, lines 840-850 for flatbuffers flags)
    - bin/start_backend.sh (line 109 area for LD_LIBRARY_PATH)
  </read_first>
  <action>
1. **.gitignore**: Remove the two lines at the end of the file:
   ```
   # GSD planning files
   .planning/
   ```
   Also ensure the `handbook/plans/local/` entry added on this branch remains (it is a legitimate entry).

2. **thirdparty/build-thirdparty.sh**: Revert two local workaround additions:
   - Line ~780 (rocksdb `EXTRA_CXXFLAGS`): Remove the added `-Wno-array-bounds -Wno-stringop-overread` flags. The line should read:
     ```
     EXTRA_CXXFLAGS="-fPIC -Wno-redundant-move -Wno-deprecated-copy -Wno-stringop-truncation -Wno-pessimizing-move -I ${TP_INCLUDE_DIR} -I ${TP_INCLUDE_DIR}/snappy ${FILE_PREFIX_MAP_OPTION}" \
     ```
   - Line ~846 (flatbuffers): Remove the added `CXXFLAGS="-Wno-stringop-overflow -Wno-array-bounds"` line entirely. The flatbuffers build block should have only:
     ```
     LDFLAGS="-static-libstdc++ -static-libgcc" \
     ${CMAKE_CMD} .. -G "${CMAKE_GENERATOR}" -DFLATBUFFERS_BUILD_TESTS=OFF
     ```
   Do NOT touch any other lines in this file, especially the `build_adbc()` function and the `ARROW_THRIFT_URL` / `thrift_SOURCE=SYSTEM` additions which are legitimate ADBC changes.

3. **bin/start_backend.sh**: The diff against main shows line 109 changed from:
   ```
   export LD_LIBRARY_PATH=$JAVA_HOME/lib/server:$JAVA_HOME/lib:$LD_LIBRARY_PATH
   ```
   to:
   ```
   export LD_LIBRARY_PATH=$STARROCKS_HOME/lib:$JAVA_HOME/lib/server:$JAVA_HOME/lib:$LD_LIBRARY_PATH
   ```
   The addition of `$STARROCKS_HOME/lib` is necessary for ADBC because `libadbc_driver_manager.so` is installed to `$STARROCKS_HOME/lib` by BE packaging. This is an intentional ADBC change. **KEEP this change.** Add a comment above it:
   ```bash
   # STARROCKS_HOME/lib needed for runtime-loaded libraries (e.g. libadbc_driver_manager.so)
   export LD_LIBRARY_PATH=$STARROCKS_HOME/lib:$JAVA_HOME/lib/server:$JAVA_HOME/lib:$LD_LIBRARY_PATH
   ```
  </action>
  <verify>
    <automated>! grep -q "\.planning/" .gitignore && ! grep -q "Wno-array-bounds" thirdparty/build-thirdparty.sh && ! grep -q "Wno-stringop-overread" thirdparty/build-thirdparty.sh && ! grep -q "Wno-stringop-overflow" thirdparty/build-thirdparty.sh && grep -q "STARROCKS_HOME/lib" bin/start_backend.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` does not contain `.planning/`
    - `.gitignore` still contains `handbook/plans/local/`
    - `thirdparty/build-thirdparty.sh` does not contain `-Wno-array-bounds`, `-Wno-stringop-overread`, or `-Wno-stringop-overflow`
    - `thirdparty/build-thirdparty.sh` still contains the `build_adbc()` function
    - `bin/start_backend.sh` still contains `$STARROCKS_HOME/lib` in the `LD_LIBRARY_PATH` line
    - `bin/start_backend.sh` has a comment explaining why `STARROCKS_HOME/lib` is on the path
  </acceptance_criteria>
  <done>Build workarounds reverted, .gitignore cleaned, start_backend.sh LD_LIBRARY_PATH intentionally kept with explanatory comment.</done>
</task>

<task type="auto">
  <name>Task 3: Remove GSD requirement tags from BE ADBC source comments</name>
  <files>be/src/exec/adbc_scanner.cpp, be/src/exec/adbc_scanner.h, be/src/connector/adbc_connector.cpp, be/src/exec/adbc_driver_registry.h</files>
  <read_first>
    - be/src/exec/adbc_scanner.cpp (full file -- 407 lines, identify all GSD tag comments)
    - be/src/exec/adbc_scanner.h (full file -- 93 lines, identify all GSD tag comments)
    - be/src/connector/adbc_connector.cpp (full file -- 168 lines, identify all GSD tag comments)
    - be/src/exec/adbc_driver_registry.h (full file -- 62 lines, identify all GSD tag comments)
  </read_first>
  <action>
Remove GSD requirement tags from comments in all four BE files. The rule: remove only the `(per XX-NN)` or `(XX-NN)` tag text; keep all rationale text surrounding it. If a comment consists ONLY of a tag with no rationale, delete the entire comment line.

**adbc_scanner.cpp** -- expected edits:
- `// 6. Forward ALL adbc_options before Init (per BE-02)` -> `// 6. Forward ALL adbc_options before Init`
- `// 8. Connection (per BE-03: per-fragment, never cached)` -> `// 8. Connection (per-fragment, never cached)`
- Any other `(per BE-NN)`, `(per D-NN)`, `(per STK-NN)` tags

**adbc_scanner.h** -- expected edits:
- `// Takes context by const ref (per D-01). Scanner copies the context.` -> `// Takes context by const ref. Scanner copies the context.`
- `// Arrow C stream -- RAII-managed (per D-08, BE-08)` -> `// Arrow C stream -- RAII-managed`
- Any other tags

**adbc_connector.cpp** -- expected edits:
- `// Read connection params from ADBCTableDescriptor (per D-04, D-05)` -> `// Read connection params from ADBCTableDescriptor`
- `// Build ADBCScanContext from table descriptor + scan node (per D-01)` -> `// Build ADBCScanContext from table descriptor + scan node`
- `// Create scanner with context struct (per D-01)` -> `// Create scanner with context struct`
- Any other tags

**adbc_driver_registry.h** -- expected edits:
- `// resident until process exit (per BE-05, BE-06).` -> `// resident until process exit.`
- Any other tags

Search broadly for patterns: `(per [A-Z]+-[0-9]+)`, `(D-[0-9]+)`, `(BE-[0-9]+)`, `(STK-[0-9]+)`, `(PROP-[0-9]+)`, `(VAL-[0-9]+)`, `(META-[0-9]+)` and remove each match while preserving surrounding text.
  </action>
  <verify>
    <automated>! grep -rEn '\(per [A-Z]+-[0-9]+' be/src/exec/adbc_scanner.cpp be/src/exec/adbc_scanner.h be/src/connector/adbc_connector.cpp be/src/exec/adbc_driver_registry.h && ! grep -rEn '\(D-[0-9]+' be/src/exec/adbc_scanner.cpp be/src/exec/adbc_scanner.h be/src/connector/adbc_connector.cpp be/src/exec/adbc_driver_registry.h && ! grep -rEn '\(BE-[0-9]+' be/src/exec/adbc_scanner.cpp be/src/exec/adbc_scanner.h be/src/connector/adbc_connector.cpp be/src/exec/adbc_driver_registry.h && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Zero matches for `(per D-NN)`, `(per BE-NN)`, `(per STK-NN)`, `(D-NN)`, `(BE-NN)` patterns in all four files
    - All rationale text surrounding the removed tags is preserved (e.g., "per-fragment, never cached" text still present in adbc_scanner.cpp)
    - `grep -c "per-fragment" be/src/exec/adbc_scanner.cpp` returns at least 1 (rationale preserved)
    - `grep -c "RAII-managed" be/src/exec/adbc_scanner.h` returns at least 1 (rationale preserved)
    - `grep -c "ADBCTableDescriptor" be/src/connector/adbc_connector.cpp` returns at least 1 (rationale preserved)
    - `grep -c "resident until process exit" be/src/exec/adbc_driver_registry.h` returns at least 1 (rationale preserved)
  </acceptance_criteria>
  <done>All GSD requirement tags removed from BE ADBC source files. Design rationale text preserved in every case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. This plan modifies build configuration and removes dead code/comments. No new input handling, no API changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T (Tampering) | build-thirdparty.sh | accept | Reverting to main-branch flags. Build flags are developer-controlled, not user-facing. |
</threat_model>

<verification>
After all three tasks:
```bash
# Verify smoke test deletion
test ! -f be/test/exec/adbc_smoke_test.cpp && test ! -f be/test/exec/ADBC_SMOKE_TEST_README.md

# Verify no GSD tags remain in BE ADBC code
! grep -rEn '\(per [A-Z]+-[0-9]+|\(D-[0-9]+|\(BE-[0-9]+|\(STK-[0-9]+' \
  be/src/exec/adbc_scanner.cpp be/src/exec/adbc_scanner.h \
  be/src/connector/adbc_connector.cpp be/src/exec/adbc_driver_registry.h

# Verify .gitignore clean
! grep -q '\.planning/' .gitignore

# Verify build workarounds reverted
! grep -q 'Wno-array-bounds' thirdparty/build-thirdparty.sh
```
</verification>

<success_criteria>
- BE smoke test files (`adbc_smoke_test.cpp`, `ADBC_SMOKE_TEST_README.md`) deleted
- CMakeLists.txt smoke test target removed
- `.gitignore` `.planning/` entry removed
- `thirdparty/build-thirdparty.sh` local `-Wno-*` workarounds reverted
- `bin/start_backend.sh` LD_LIBRARY_PATH kept with explanatory comment
- All GSD requirement tags removed from 4 BE source files with rationale preserved
</success_criteria>

<output>
After completion, create `.planning/phases/06-pr-review-resolution-test-hardening/06-01-SUMMARY.md`
</output>
