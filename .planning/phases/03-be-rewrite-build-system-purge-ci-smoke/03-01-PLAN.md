---
phase: 03-be-rewrite-build-system-purge-ci-smoke
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - thirdparty/build-thirdparty.sh
  - thirdparty/vars-x86_64.sh
  - thirdparty/vars-aarch64.sh
  - thirdparty/package-manifest.sh
  - be/CMakeLists.txt
autonomous: true
requirements: [STK-04, STK-05, STK-06, STK-07]
must_haves:
  truths:
    - "build_adbc() produces libadbc_driver_manager.so (shared) instead of .a (static)"
    - "build_go() function no longer exists in build-thirdparty.sh"
    - "No GO_SOURCE, GO_DOWNLOAD, GO_NAME, GO_MD5SUM vars in thirdparty scripts"
    - "be/CMakeLists.txt line 638 links adbc_driver_manager, line 637 still links arrow_flight_sql"
    - "libadbc_driver_manager.so is copied into BE output directory for runtime resolution"
  artifacts:
    - path: "thirdparty/build-thirdparty.sh"
      provides: "Rewritten build_adbc() with shared lib flags, no build_go()"
      contains: "ADBC_BUILD_SHARED=ON"
    - path: "be/CMakeLists.txt"
      provides: "Shared lib link target swap"
      contains: "adbc_driver_manager"
  key_links:
    - from: "thirdparty/build-thirdparty.sh"
      to: "thirdparty/installed/lib64/libadbc_driver_manager.so"
      via: "cmake install"
      pattern: "ADBC_DRIVER_MANAGER=ON"
    - from: "be/CMakeLists.txt"
      to: "thirdparty/installed/lib64/libadbc_driver_manager.so"
      via: "link dependency"
      pattern: "adbc_driver_manager"
---

<objective>
Rewrite the thirdparty ADBC build to produce `libadbc_driver_manager.so` (shared) instead of the static Flight SQL library, purge the Go toolchain from all thirdparty scripts, swap the BE CMakeLists link target, and ensure the shared library is discoverable at runtime.

Purpose: The scanner rewrite in Plans 02-03 requires linking against the shared driver manager. This plan creates the foundational build artifact.
Output: Shared `libadbc_driver_manager.so` in `thirdparty/installed/lib64/`, Go toolchain fully removed, BE links the manager.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-be-rewrite-build-system-purge-ci-smoke/03-CONTEXT.md
@.planning/phases/03-be-rewrite-build-system-purge-ci-smoke/03-RESEARCH.md

<interfaces>
<!-- Key build system locations verified by research -->

From thirdparty/build-thirdparty.sh (lines 954-996):
- build_go() at line 955: Downloads and installs Go tarball. DELETE entirely.
- build_adbc() at line 964: Subshell with GOROOT/GOPATH/GOCACHE, builds static .a. REWRITE.

From thirdparty/vars-x86_64.sh (lines 54-58):
- GO_DOWNLOAD, GO_NAME, GO_SOURCE, GO_MD5SUM — DELETE these 4 lines.

From thirdparty/vars-aarch64.sh (lines 59-63):
- GO_DOWNLOAD, GO_NAME, GO_SOURCE, GO_MD5SUM — DELETE these 4 lines.

From thirdparty/package-manifest.sh (line 74):
- "go" entry in STARROCKS_THIRDPARTY_ALL_PACKAGES array — DELETE.

From be/CMakeLists.txt (lines 635-639):
- Line 637: arrow_flight_sql — KEEP (unrelated outbound Arrow Flight SQL server)
- Line 638: adbc_driver_flightsql — SWAP to adbc_driver_manager

From be/CMakeLists.txt (line 235):
- CMAKE_SKIP_RPATH TRUE — means .so must be co-located with binary at runtime
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite build_adbc(), delete build_go(), purge Go vars</name>
  <files>thirdparty/build-thirdparty.sh, thirdparty/vars-x86_64.sh, thirdparty/vars-aarch64.sh, thirdparty/package-manifest.sh</files>
  <read_first>
    thirdparty/build-thirdparty.sh
    thirdparty/vars-x86_64.sh
    thirdparty/vars-aarch64.sh
    thirdparty/package-manifest.sh
    thirdparty/vars.sh
  </read_first>
  <action>
**build-thirdparty.sh changes:**

1. DELETE the entire `build_go()` function (starts at line 955, ends before `build_adbc()`). Also delete any call to `build_go` in the build sequence at the bottom of the file.

2. REWRITE `build_adbc()` (starts at line 964). Replace the entire function body with:
```bash
build_adbc() {
    check_if_source_exist $ADBC_SOURCE
    cd $TP_SOURCE_DIR/$ADBC_SOURCE/c
    mkdir -p build && cd build
    rm -rf CMakeCache.txt CMakeFiles/
    ${CMAKE_CMD} \
        -DADBC_DRIVER_MANAGER=ON \
        -DADBC_DRIVER_FLIGHTSQL=OFF \
        -DADBC_DRIVER_SQLITE=ON \
        -DADBC_BUILD_SHARED=ON \
        -DADBC_BUILD_STATIC=OFF \
        -DADBC_BUILD_TESTS=OFF \
        -DADBC_BUILD_BENCHMARKS=OFF \
        -DADBC_BUILD_EXAMPLES=OFF \
        -DCMAKE_INSTALL_PREFIX=$TP_INSTALL_DIR \
        -DCMAKE_INSTALL_LIBDIR=lib64 \
        -DCMAKE_PREFIX_PATH="${TP_INSTALL_DIR}" \
        -DCMAKE_BUILD_TYPE=Release \
        -G "${CMAKE_GENERATOR}" ..
    ${BUILD_SYSTEM} -j$PARALLEL
    ${BUILD_SYSTEM} install
}
```

Key differences from old function:
- No subshell `(...)` wrapper
- No GOROOT/GOPATH/GOCACHE env vars
- `-DADBC_DRIVER_FLIGHTSQL=OFF` (was ON)
- `-DADBC_BUILD_SHARED=ON` (was OFF)
- `-DADBC_BUILD_STATIC=OFF` (was ON)
- `-DADBC_DRIVER_SQLITE=ON` (new — needed for smoke tests in Plan 04)
- No `-DArrowFlight_DIR` or `-DArrowFlightSQL_DIR` (not needed when FlightSQL driver is off)
- If the existing function uses `-DArrow_DIR`, keep it if present. If not present, omit it.

**vars-x86_64.sh changes:**
Delete the 4 Go-related lines (around lines 54-58): `GO_DOWNLOAD`, `GO_NAME`, `GO_SOURCE`, `GO_MD5SUM`. Leave `ADBC_SOURCE` and everything else untouched.

**vars-aarch64.sh changes:**
Delete the 4 Go-related lines (around lines 59-63): `GO_DOWNLOAD`, `GO_NAME`, `GO_SOURCE`, `GO_MD5SUM`.

**package-manifest.sh changes:**
Delete the `go` entry from the `STARROCKS_THIRDPARTY_ALL_PACKAGES` array (line 74). Keep the `adbc` entry.
  </action>
  <verify>
    <automated>grep -c "build_go" thirdparty/build-thirdparty.sh | grep -q "^0$" && grep -q "ADBC_BUILD_SHARED=ON" thirdparty/build-thirdparty.sh && grep -q "ADBC_DRIVER_FLIGHTSQL=OFF" thirdparty/build-thirdparty.sh && ! grep -q "GO_SOURCE" thirdparty/vars-x86_64.sh && ! grep -q "GO_SOURCE" thirdparty/vars-aarch64.sh && ! grep -q '"go"' thirdparty/package-manifest.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - thirdparty/build-thirdparty.sh contains no `build_go` function or call
    - thirdparty/build-thirdparty.sh build_adbc() contains `ADBC_BUILD_SHARED=ON`
    - thirdparty/build-thirdparty.sh build_adbc() contains `ADBC_DRIVER_FLIGHTSQL=OFF`
    - thirdparty/build-thirdparty.sh build_adbc() contains `ADBC_DRIVER_SQLITE=ON`
    - thirdparty/build-thirdparty.sh build_adbc() contains no `GOROOT`, `GOPATH`, `GOCACHE`
    - thirdparty/vars-x86_64.sh contains no `GO_SOURCE`, `GO_DOWNLOAD`, `GO_NAME`, `GO_MD5SUM`
    - thirdparty/vars-aarch64.sh contains no `GO_SOURCE`, `GO_DOWNLOAD`, `GO_NAME`, `GO_MD5SUM`
    - thirdparty/package-manifest.sh does not contain `"go"` in the packages array
    - thirdparty/vars.sh still contains `ADBC_SOURCE` (unchanged)
  </acceptance_criteria>
  <done>build_adbc() produces shared libs, Go toolchain fully purged from all thirdparty scripts</done>
</task>

<task type="auto">
  <name>Task 2: Swap CMakeLists link target and ensure runtime .so discoverability</name>
  <files>be/CMakeLists.txt</files>
  <read_first>
    be/CMakeLists.txt
  </read_first>
  <action>
**CMakeLists.txt line 638 swap (per D-13, STK-06):**

Find the `STARROCKS_DEPENDENCIES` list (around lines 635-639). It currently reads:
```
    arrow_flight_sql        # line 637 — KEEP
    adbc_driver_flightsql   # line 638 — CHANGE
```

Change line 638 from `adbc_driver_flightsql` to `adbc_driver_manager`. Do NOT touch line 637 (`arrow_flight_sql`) — that is used by the unrelated outbound Arrow Flight SQL server (`service_be/arrow_flight_sql_service.cpp`).

**Runtime .so discoverability (STK-07):**

Search `be/CMakeLists.txt` for the section that copies thirdparty shared libraries into the BE output directory (look for patterns like `file(GLOB ...)` or `install(FILES ...)` that copy `.so` files from `thirdparty/installed/lib64/`). Add `libadbc_driver_manager.so*` to the copy list following the existing pattern.

If no explicit copy step exists for individual `.so` files in CMakeLists.txt, search for the packaging/install script instead — it may be in `build.sh` or a separate cmake install target. The goal: `libadbc_driver_manager.so` ends up in the same directory as `starrocks_be` at runtime. Follow whatever pattern exists for other thirdparty shared libs (e.g., `libjemalloc.so`, `librdkafka.so`).

If the existing pattern is simply that all `.so` files from `thirdparty/installed/lib64/` are bulk-copied, verify that switching from `.a` to `.so` means the shared lib will be picked up automatically. In that case, no additional CMake change is needed beyond the line 638 swap.
  </action>
  <verify>
    <automated>grep -n "adbc_driver_manager" be/CMakeLists.txt && grep -n "arrow_flight_sql" be/CMakeLists.txt && ! grep -q "adbc_driver_flightsql" be/CMakeLists.txt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - be/CMakeLists.txt contains `adbc_driver_manager` (the new shared lib target)
    - be/CMakeLists.txt does NOT contain `adbc_driver_flightsql`
    - be/CMakeLists.txt still contains `arrow_flight_sql` on its original line (preserved)
    - libadbc_driver_manager.so will be discoverable by the BE binary at runtime (via existing bulk copy, explicit copy step, or rpath — verify which pattern applies)
  </acceptance_criteria>
  <done>BE links adbc_driver_manager (shared) instead of adbc_driver_flightsql (static), .so is runtime-discoverable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build system → runtime binary | Shared library linkage must resolve correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | build_adbc() cmake flags | mitigate | Verify ADBC_DRIVER_FLIGHTSQL=OFF and ADBC_BUILD_SHARED=ON in build script; no Go env vars present |
| T-03-02 | Denial of service | Missing .so at runtime | mitigate | STK-07 ensures libadbc_driver_manager.so is copied to BE output dir; verify via acceptance criteria |
</threat_model>

<verification>
1. `grep -c "build_go" thirdparty/build-thirdparty.sh` returns 0
2. `grep "ADBC_BUILD_SHARED=ON" thirdparty/build-thirdparty.sh` matches
3. `grep "adbc_driver_manager" be/CMakeLists.txt` matches
4. `grep "adbc_driver_flightsql" be/CMakeLists.txt` returns nothing
5. `grep "arrow_flight_sql" be/CMakeLists.txt` still matches (preserved)
6. No `GO_SOURCE` in vars-x86_64.sh or vars-aarch64.sh
</verification>

<success_criteria>
- build_adbc() produces libadbc_driver_manager.so as a shared library
- Go toolchain completely removed from thirdparty build
- BE links against adbc_driver_manager (shared) instead of adbc_driver_flightsql (static)
- arrow_flight_sql link at line 637 preserved unchanged
- Runtime .so discoverability ensured
</success_criteria>

<output>
After completion, create `.planning/phases/03-be-rewrite-build-system-purge-ci-smoke/03-01-SUMMARY.md`
</output>
