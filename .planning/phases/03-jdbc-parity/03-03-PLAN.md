---
phase: 03-jdbc-parity
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - fe/fe-core/src/test/java/com/starrocks/connector/adbc/ADBCMetadataTest.java
autonomous: true
requirements: [MV-01, MV-02, MV-04]

must_haves:
  truths:
    - "CREATE MATERIALIZED VIEW over ADBC table succeeds (ADBC in SUPPORTED_TABLE_TYPE + traits registered)"
    - "REFRESH MATERIALIZED VIEW fetches data through existing ADBC scan path"
    - "Query optimizer can rewrite queries to use MV over ADBC tables"
  artifacts:
    - path: "fe/fe-core/src/test/java/com/starrocks/connector/adbc/ADBCMetadataTest.java"
      provides: "MV integration tests for ADBC"
      contains: "testMVSupport"
  key_links:
    - from: "MaterializedViewAnalyzer.SUPPORTED_TABLE_TYPE"
      to: "Table.TableType.ADBC"
      via: "Already registered (project Phase 1: Foundation, plan 01-05)"
      pattern: "SUPPORTED_TABLE_TYPE.*ADBC"
    - from: "ConnectorPartitionTraits.TRAITS_TABLE"
      to: "ADBCPartitionTraits"
      via: "Registered in Phase 3 Plan 01"
      pattern: "Table\\.TableType\\.ADBC"
---

<objective>
Verify and test that materialized views work end-to-end over ADBC tables. MV creation, refresh, and query rewrite should work out of the box now that ADBCPartitionTraits is registered and getTableStatistics() provides row counts.

Purpose: Confirm MV-01 is satisfied through structural tests. MV-02 (refresh) and MV-04 (query rewrite) are verified structurally here; full behavioral tests (with ConnectorPlanTestBase + MockedADBCMetadata) are deferred to Phase 4: Integration Testing.
Output: Updated ADBCMetadataTest.java with MV verification tests, full build verification.
</objective>

<execution_context>
@/home/mete/.claude/get-shit-done/workflows/execute-plan.md
@/home/mete/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-jdbc-parity/03-CONTEXT.md
@.planning/phases/03-jdbc-parity/03-RESEARCH.md
@.planning/phases/03-jdbc-parity/03-01-SUMMARY.md
@.planning/phases/03-jdbc-parity/03-02-SUMMARY.md

<interfaces>
<!-- Key patterns from research -->

From fe/fe-core/src/main/java/com/starrocks/sql/analyzer/MaterializedViewAnalyzer.java (line ~178):
```java
// ADBC is already in SUPPORTED_TABLE_TYPE -- no changes needed
private static final Set<Table.TableType> SUPPORTED_TABLE_TYPE = Sets.newHashSet(
    Table.TableType.OLAP, Table.TableType.HIVE, Table.TableType.HUDI,
    Table.TableType.ICEBERG, Table.TableType.JDBC, Table.TableType.ADBC, ...
);
```

Test pattern from MvRefreshAndRewriteJDBCTest.java:
- Uses ConnectorPlanTestBase to register mocked catalogs
- Creates MV with SQL, verifies plan contains expected scan nodes
- Tests query rewrite by checking EXPLAIN output
- NOTE: This test pattern requires MockedADBCMetadata (Phase 4 scope)

Key verification: MV works because:
1. ADBC is in SUPPORTED_TABLE_TYPE (project Phase 1: Foundation, plan 01-05)
2. ADBCPartitionTraits registered in TRAITS_TABLE (Phase 3 Plan 01)
3. getTableStatistics() returns row count (Phase 3 Plan 02)
4. ADBC scan path works end-to-end (project Phase 2: Scanning)

MV-02 (refresh) and MV-04 (query rewrite) behavioral testing deferred:
- Full behavioral MV tests (MvRefreshAndRewriteADBCTest) require MockedADBCMetadata
  registered in ConnectorPlanTestBase, which is Phase 4: Integration Testing scope
- This plan verifies the structural prerequisites are met (traits + SUPPORTED_TABLE_TYPE)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MV support verification tests and run full build</name>
  <files>
    fe/fe-core/src/test/java/com/starrocks/connector/adbc/ADBCMetadataTest.java
  </files>
  <action>
Add tests to ADBCMetadataTest.java that verify the MV framework correctly handles ADBC tables. These are structural/registration tests proving the wiring is correct. Full behavioral MV tests (MvRefreshAndRewriteADBCTest with CREATE MV, REFRESH, EXPLAIN rewrite verification) require MockedADBCMetadata registered in ConnectorPlanTestBase, which is Phase 4: Integration Testing scope.

Add these test methods:

1. `testMVSupportedTableType()` -- Verify `Table.TableType.ADBC` is in `MaterializedViewAnalyzer`'s SUPPORTED_TABLE_TYPE set. Access it via reflection if the set is private, or use `ConnectorPartitionTraits.isSupported(Table.TableType.ADBC)` as a proxy (if traits are registered, MV should work).

2. `testADBCPartitionTraitsGetTableName()` -- Create an ADBCTable with known catalogName/dbName/tableName. Use reflection or direct instantiation to create an ADBCPartitionTraits, set its `table` field (protected in ConnectorPartitionTraits), and verify `getTableName()` returns the correct "catalog.db.table" format.

3. `testADBCPartitionTraitsGetPartitions()` -- Create ADBCPartitionTraits, verify `getPartitions(anyList)` returns empty list.

4. `testADBCPartitionTraitsCreateEmptyKey()` -- Verify `createEmptyKey()` returns a `NullablePartitionKey` instance.

For setting the `table` field on ADBCPartitionTraits, use `ConnectorPartitionTraits.build(Table.TableType.ADBC)` which returns a traits instance, then call `.setTable(adbcTable)` (there should be a `setTable` or `build` pattern -- check `ConnectorPartitionTraits.build()`).

Add imports:
- `com.starrocks.connector.ConnectorPartitionTraits`
- `com.starrocks.connector.partitiontraits.ADBCPartitionTraits`
- `com.starrocks.catalog.NullablePartitionKey`

After all tests pass, run full FE compile to verify no regressions:
`cd fe && mvn compile -pl fe-core -am -DskipTests`

NOTE: MV-02 (refresh behavioral) and MV-04 (query rewrite behavioral) testing is explicitly deferred to Phase 4: Integration Testing. That phase will create MvRefreshAndRewriteADBCTest using MockedADBCMetadata + ConnectorPlanTestBase to test actual CREATE MV, REFRESH, and EXPLAIN rewrite flows. The structural tests here prove all prerequisites are wired correctly.
  </action>
  <verify>
    <automated>cd /home/mete/Coding/starrocks/fe && mvn test -pl fe-core -Dtest=ADBCMetadataTest -am -q 2>&1 | tail -10</automated>
  </verify>
  <done>All MV verification tests pass. ADBCPartitionTraits works correctly with ADBCTable. Full FE compiles cleanly. MV-01 verified structurally. MV-02/MV-04 prerequisites confirmed; behavioral tests deferred to Phase 4.</done>
</task>

</tasks>

<verification>
- All ADBCMetadataTest tests pass (existing + new MV tests)
- FE compiles cleanly: `cd fe && mvn compile -pl fe-core -am -DskipTests`
- ADBCPartitionTraits.getTableName() returns correct format
- ADBCPartitionTraits.isSupportPCTRefresh() returns false
- ADBCPartitionTraits.getPartitions() returns empty list
</verification>

<success_criteria>
MV framework recognizes ADBC tables through partition traits registration (MV-01 verified). Structural tests prove the wiring is correct. Full MV behavioral tests -- MvRefreshAndRewriteADBCTest covering MV-02 (refresh) and MV-04 (query rewrite) -- are explicitly Phase 4: Integration Testing scope, requiring MockedADBCMetadata + ConnectorPlanTestBase.
</success_criteria>

<output>
After completion, create `.planning/phases/03-jdbc-parity/03-03-SUMMARY.md`
</output>
