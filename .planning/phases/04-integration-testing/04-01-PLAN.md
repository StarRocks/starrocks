---
phase: 04-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fe/fe-core/src/test/java/com/starrocks/connector/adbc/MockedADBCMetadata.java
  - fe/fe-core/src/test/java/com/starrocks/sql/plan/ConnectorPlanTestBase.java
  - fe/fe-core/src/test/java/com/starrocks/sql/plan/ADBCScanPlanTest.java
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "MockedADBCMetadata is registered in ConnectorPlanTestBase alongside JDBC, Hive, Iceberg, etc."
    - "EXPLAIN output for ADBC queries shows ADBC_SCAN_NODE (not JDBC_SCAN_NODE)"
    - "Plan tests verify column pruning, predicate pushdown, and limit pushdown in EXPLAIN output"
    - "Cross-catalog join between ADBC table and native StarRocks table produces valid plan"
  artifacts:
    - path: "fe/fe-core/src/test/java/com/starrocks/connector/adbc/MockedADBCMetadata.java"
      provides: "Mocked ConnectorMetadata returning ADBCTable instances"
      contains: "class MockedADBCMetadata implements ConnectorMetadata"
    - path: "fe/fe-core/src/test/java/com/starrocks/sql/plan/ADBCScanPlanTest.java"
      provides: "Plan tests asserting ADBC_SCAN_NODE EXPLAIN output"
      contains: "class ADBCScanPlanTest extends ConnectorPlanTestBase"
  key_links:
    - from: "ConnectorPlanTestBase.java"
      to: "MockedADBCMetadata.java"
      via: "mockADBCCatalogImpl() registration"
      pattern: "mockADBCCatalogImpl"
    - from: "ADBCScanPlanTest.java"
      to: "MockedADBCMetadata.MOCKED_ADBC_CATALOG_NAME"
      via: "SQL queries referencing adbc0 catalog"
      pattern: "adbc0\\.test_db0\\.tbl0"
---

<objective>
Create MockedADBCMetadata for FE plan tests and register it in ConnectorPlanTestBase, then write ADBCScanPlanTest verifying EXPLAIN output shows ADBC_SCAN_NODE with correct pushdowns.

Purpose: Enable FE plan-level testing of the ADBC scan path without requiring a real ADBC connection
Output: MockedADBCMetadata.java, updated ConnectorPlanTestBase.java, ADBCScanPlanTest.java
</objective>

<execution_context>
@/home/mete/.claude/get-shit-done/workflows/execute-plan.md
@/home/mete/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-testing/04-RESEARCH.md

@fe/fe-core/src/test/java/com/starrocks/connector/jdbc/MockedJDBCMetadata.java
@fe/fe-core/src/test/java/com/starrocks/sql/plan/ConnectorPlanTestBase.java
@fe/fe-core/src/test/java/com/starrocks/sql/plan/JDBCIdentifierQuoteTest.java
@fe/fe-core/src/main/java/com/starrocks/catalog/ADBCTable.java
@fe/fe-core/src/main/java/com/starrocks/planner/ADBCScanNode.java
@fe/fe-core/src/main/java/com/starrocks/connector/adbc/ADBCMetadata.java

<interfaces>
<!-- Key types and contracts the executor needs. -->

From fe/fe-core/src/main/java/com/starrocks/catalog/ADBCTable.java:
```java
public class ADBCTable extends Table {
    public ADBCTable(long id, String name, List<Column> fullSchema, String dbName,
                     String catalogName, Map<String, String> properties)
    // No partition columns parameter (unlike JDBCTable)
    // Properties must include "adbc.driver" and "adbc.url"
}
```

From ConnectorPlanTestBase.java registration pattern:
```java
// mockJDBCCatalogImpl pattern to follow:
// 1. Create properties map with type, driver, url
// 2. CatalogMgr.createCatalog("adbc", catalogName, "", properties)
// 3. new MockedADBCMetadata(properties)
// 4. metadataMgr.registerMockedMetadata(catalogName, metadata)
```

From ADBCScanNode EXPLAIN output:
```java
// Display name: "SCAN ADBC" (not "SCAN JDBC")
// Uses double-quote identifiers by default: "col" not `col`
// Shows QUERY: field with pushed-down SQL
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MockedADBCMetadata and register in ConnectorPlanTestBase</name>
  <files>
    fe/fe-core/src/test/java/com/starrocks/connector/adbc/MockedADBCMetadata.java,
    fe/fe-core/src/test/java/com/starrocks/sql/plan/ConnectorPlanTestBase.java
  </files>
  <action>
Create MockedADBCMetadata.java mirroring MockedJDBCMetadata exactly but for ADBC:

1. Package: com.starrocks.connector.adbc
2. Implements ConnectorMetadata
3. Constants: MOCKED_ADBC_CATALOG_NAME = "adbc0", MOCKED_DB_NAME = "test_db0", MOCKED_TABLE_NAME0 = "tbl0"
4. getTableType() returns Table.TableType.ADBC
5. getTable() returns ADBCTable instances (NOT JDBCTable). ADBCTable constructor takes (id, name, fullSchema, dbName, catalogName, properties) -- note: NO partitionColumns parameter unlike JDBCTable.
6. Schema for tbl0: columns a (VARCHAR), b (VARCHAR), c (INT), d (VARCHAR) -- same as JDBC tbl0 for consistency
7. listDbNames() returns ["test_db0"]
8. listTableNames() returns ["tbl0"]
9. getDb() returns new Database with generated id
10. listPartitionNames() returns empty list (ADBC has no partition support per Phase 3 decisions)
11. getPartitions() returns empty list
12. Use ReentrantReadWriteLock for thread safety (same pattern as MockedJDBCMetadata)
13. Properties map stored from constructor, must include "adbc.driver" and "adbc.url"

Then update ConnectorPlanTestBase.java:
1. Add import for MockedADBCMetadata
2. In mockAllCatalogs(): add call to mockADBCCatalogImpl(metadataMgr) after mockKuduCatalogImpl
3. In mockCatalog() switch: add case for MockedADBCMetadata.MOCKED_ADBC_CATALOG_NAME -> mockADBCCatalogImpl(metadataMgr)
4. In dropAllCatalogs(): add dropCatalog(MockedADBCMetadata.MOCKED_ADBC_CATALOG_NAME)
5. Add private static void mockADBCCatalogImpl(MockedMetadataMgr metadataMgr) method:
   - Properties: "type" -> "adbc", "adbc.driver" -> "flight_sql", "adbc.url" -> "grpc://127.0.0.1:31337"
   - CatalogMgr.createCatalog("adbc", MOCKED_ADBC_CATALOG_NAME, "", properties)
   - new MockedADBCMetadata(properties) -> registerMockedMetadata

IMPORTANT: The first argument to createCatalog must be "adbc" (the catalog type string), NOT "jdbc". This ensures the ADBC connector path is exercised. Verify ADBCTable.getTableType() returns ADBC so the optimizer creates LogicalADBCScanOperator (not LogicalJDBCScanOperator).
  </action>
  <verify>
    <automated>cd /home/mete/Coding/starrocks/fe && mvn compile -pl fe-core -am -DskipTests -q 2>&1 | tail -5</automated>
  </verify>
  <done>MockedADBCMetadata.java exists and compiles. ConnectorPlanTestBase registers "adbc0" catalog with ADBC type. No compilation errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create ADBCScanPlanTest with EXPLAIN assertions</name>
  <files>fe/fe-core/src/test/java/com/starrocks/sql/plan/ADBCScanPlanTest.java</files>
  <action>
Create ADBCScanPlanTest.java extending ConnectorPlanTestBase with these test methods:

1. testADBCSelectAll: `select a, b, c, d from adbc0.test_db0.tbl0` -- assert plan contains "SCAN ADBC"
2. testADBCColumnPruning: `select a, c from adbc0.test_db0.tbl0` -- assert plan contains "SCAN ADBC" and shows only requested columns in QUERY
3. testADBCPredicatePushdown: `select a from adbc0.test_db0.tbl0 where c > 10` -- assert plan contains "SCAN ADBC" and contains WHERE clause in pushed SQL
4. testADBCLimitPushdown: `select a from adbc0.test_db0.tbl0 limit 5` -- assert plan shows LIMIT in pushed SQL
5. testADBCCombinedPushdown: `select a from adbc0.test_db0.tbl0 where b = 'x' limit 10` -- assert both predicate and limit pushed
6. testADBCCrossCatalogJoin: `select t1.a, t2.v1 from adbc0.test_db0.tbl0 t1 join test.t0 t2 on t1.c = t2.v1` -- assert plan contains both "SCAN ADBC" and "OlapScanNode" (cross-catalog join with native table, t0 is created by PlanTestBase.beforeClass())

Pattern: Use getFragmentPlan(sql) to get EXPLAIN output, then assertContains(plan, expectedString) for assertions. Follow JDBCIdentifierQuoteTest.java style exactly.

Note on identifiers: ADBCScanNode uses double-quote identifiers by default (standard SQL), so EXPLAIN output will show `"a"`, `"test_db0"."tbl0"` etc. Assert double-quotes, not backticks.

BeforeAll: call ConnectorPlanTestBase.beforeClass() (this sets up all mocked catalogs including the new ADBC one).
  </action>
  <verify>
    <automated>cd /home/mete/Coding/starrocks/fe && mvn test -Dtest=ADBCScanPlanTest -pl fe-core -q 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 plan test methods pass. EXPLAIN output shows "SCAN ADBC" with correct pushdowns (column pruning, predicate, limit). Cross-catalog join plan contains both ADBC and Olap scan nodes.</done>
</task>

</tasks>

<verification>
1. `cd fe && mvn test -Dtest=ADBCScanPlanTest -pl fe-core` -- all tests pass
2. EXPLAIN output contains "SCAN ADBC" (not "SCAN JDBC")
3. Column pruning shows only selected columns in QUERY field
4. Predicate pushdown shows WHERE clause in QUERY field
5. Limit pushdown shows LIMIT in QUERY field
</verification>

<success_criteria>
- MockedADBCMetadata registered in ConnectorPlanTestBase with catalog name "adbc0"
- ADBCScanPlanTest has 6+ test methods covering: select all, column pruning, predicate pushdown, limit pushdown, combined pushdown, cross-catalog join
- All plan tests pass (`mvn test -Dtest=ADBCScanPlanTest`)
- EXPLAIN output consistently shows ADBC_SCAN_NODE with double-quote identifiers
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-testing/04-01-SUMMARY.md`
</output>
