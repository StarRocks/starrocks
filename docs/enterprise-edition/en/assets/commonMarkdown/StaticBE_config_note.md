### Configure BE static parameters

You can only set the static parameters of a BE by changing them in the **`starrocksBeSpec.config`** section of `values.yaml`, and restarting the BEs to allow the changes to take effect.

