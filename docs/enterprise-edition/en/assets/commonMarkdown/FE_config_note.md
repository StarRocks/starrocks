
:::note
The configuration changes made with `ADMIN SET FRONTEND` will be restored to the default values in the 
**`starrocksFESpec.config`** section of `values.yaml` file after the FE restarts. Therefore, we recommend that you also modify the configuration items in `values.yaml` if you want the changes to be permanent.
:::
