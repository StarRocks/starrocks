import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

当前教程可以使用以下三个客户端进行测试，您只需选择其中一个：

- MySQL CLI：您可以从 Docker 环境或您的本机运行此客户端。
- [DBeaver](https://dbeaver.io/download/)（社区版或专业版）
- [MySQL Workbench](https://dev.mysql.com/downloads/workbench/)

#### 配置客户端

<Tabs groupId="client">
<TabItem value="mysqlCLI" label="mysql CLI" default>

使用 MySQL CLI 的最简单方式是从 StarRocks 容器 `starrocks-fe` 中运行：

```bash
docker compose exec starrocks-fe \
mysql -P 9030 -h 127.0.0.1 -u root --prompt="StarRocks > "
```

:::tip
所有的 `docker compose` 命令必须从包含 `docker-compose.yml` 文件的目录中运行。
:::

如果您需要安装 MySQL CLI，请展开以下 **MySQL 客户端安装** 部分：

<details>

<summary>MySQL 客户端安装</summary>

- macOS：如果您使用 Homebrew 并且不需要 MySQL 服务器，请运行 `brew install mysql` 以安装 CLI。
- Linux：请检查您的 `mysql` 客户端 Repository。例如，运行 `yum install mariadb`。
- Microsoft Windows：安装 [MySQL Community Server](https://dev.mysql.com/downloads/mysql/) 后，运行提供的客户端，或在 WSL 中运行 `mysql`。

</details>

</TabItem>

<TabItem value="DBeaver" label="DBeaver" >

- 安装 [DBeaver](https://dbeaver.io/download/) 并添加连接：
![添加连接](./DBeaver-1.png)
- 配置端口、IP 和用户名，并点击 **Test Connection** 测试连接。如果测试成功，请点击 Finish：
![配置和测试](./DBeaver-2.png)

</TabItem>
<TabItem value="MySQLWorkbench" label="MySQL Workbench">

- 安装 [MySQL Workbench](https://dev.mysql.com/downloads/workbench/) 并添加连接：
- 配置端口、IP 和用户名，并点击 **Test Connection** 测试连接:
![Connection settings](./Workbench-1.png)
- 在 Workbench 检查 MySQL 版本时，会弹出以下警告。您可以忽略这些警告，并选择不再提示：
![忽略警告](./Workbench-2.png)
</TabItem>

</Tabs>
