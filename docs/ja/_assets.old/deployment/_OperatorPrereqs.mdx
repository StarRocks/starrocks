### Kubernetes 環境

このガイドを作成する際に使用した Kubernetes 環境は、3 つのノードで構成されており、それぞれに 4 つの vCPU と 16GB の RAM が搭載されています（GCP `e2-standard-4` マシン）。Kubernetes クラスターは次の `gcloud` コマンドでデプロイされました。

:::tip
このコマンドは参考用です。AWS、Azure、または他の Kubernetes プロバイダーを使用している場合は、環境に合わせて修正する必要があります。Google Cloud では、独自のプロジェクトと適切なロケーションを指定する必要があります。
:::

```bash
gcloud container --project enterprise-demo-422514 \
  clusters create ee-docs \
  --location=southamerica-west1-b \
  --machine-type e2-standard-4 --disk-size 80 --num-nodes 3
```

### Helm

Helm は Kubernetes 用のパッケージマネージャーで、アプリケーションのデプロイと管理を簡素化します。このラボでは、Helm を使用して CelerData Enterprise Edition Kubernetes operator とサンプルの StarRocks クラスターをデプロイします。

[Install helm](https://helm.sh/docs/intro/quickstart/)