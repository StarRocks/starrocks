---
displayed_sidebar: docs
description: "H3セルインデックスが五角形セルを表す場合に1を返します。"
---

# h3IsPentagon

指定したH3セルインデックスが五角形を表す場合に1を返します。H3グリッドには解像度ごとに12個の五角形（二十面体の各頂点に1つ）があり、それ以外のセルはすべて六角形です。

## Syntax

```Haskell
BOOLEAN h3IsPentagon(BIGINT h3index)
```

## パラメータ

- `h3index`: H3セルインデックス。サポートされるデータ型: BIGINT。

## 戻り値

セルが五角形の場合は1（true）、六角形の場合は0（false）を返します。引数がNULL、またはインデックスが有効なH3セルでない場合はNULLを返します。

## 例

```sql
SELECT h3IsPentagon(644721767722457330);
+-----------------------------------+
| h3IsPentagon(644721767722457330)  |
+-----------------------------------+
|                                 0 |
+-----------------------------------+
```

## キーワード

H3ISPENTAGON,H3,SPATIAL
