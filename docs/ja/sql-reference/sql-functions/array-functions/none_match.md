---
displayed_sidebar: docs
description: "NONE_MATCH関数は配列のいずれの要素も指定された述語に一致しないかどうかを返します。"
---

# none_match

配列の要素のいずれも指定された述語に一致しないかどうかを返します。

- 要素のいずれも述語に一致しない場合、`true` (1) を返します（特別なケースとして、配列が空の場合があります）。

- 1 つ以上の要素が述語に一致する場合、`false` (0) を返します。

- 述語が 1 つ以上の要素に対して NULL を返し、他のすべての要素に対して `false` を返す場合、NULL を返します。

## Syntax

```Haskell
none_match(lambda_function, arr1, arr2...)
```

`arr1` の要素のいずれも、lambda 関数内の述語に一致しないかどうかを返します。

## Parameters

- `arr1`: 一致させる配列。

- `arrN`: lambda 関数で使用されるオプションの配列。

- `lambda_function`: 値を一致させるために使用される lambda 関数。

## Return value

BOOLEAN 値を返します。

## Usage notes

- lambda 関数は [array_map()](array_map.md) の使用上の注意に従います。
- 入力配列が null または lambda 関数の結果が null の場合、null が返されます。
- `arr1` が空の場合、`true` が返されます。
- `none_match()` は [any_match()](any_match.md) の否定であり、`none_match(f, arr)` は `NOT any_match(f, arr)` と同じ結果を返します。
- この関数を MAP に適用するには、`none_match((k,v)->k>v,map)` を `none_match(map_values(transform_values((k,v)->k>v, map)))` に書き換えます。例えば、`select none_match(map_values(transform_values((k,v)->k>v, map{2:1})));` は 0 を返します。

## Examples

`x` のいずれの要素も `y` の要素より小さくないかどうかを確認します。

```Plain
select none_match((x,y) -> x < y, [1,2,8], [4,5,6]);
+---------------------------------------------------+
| none_match((x, y) -> x < y, [1, 2, 8], [4, 5, 6]) |
+---------------------------------------------------+
|                                                 0 |
+---------------------------------------------------+

select none_match((x,y) -> x < y, [11,12,8], [4,5,6]);
+-----------------------------------------------------+
| none_match((x, y) -> x < y, [11, 12, 8], [4, 5, 6]) |
+-----------------------------------------------------+
|                                                   1 |
+-----------------------------------------------------+

select none_match((x,y) -> x < y, [11,12,null], [4,5,6]);
+--------------------------------------------------------+
| none_match((x, y) -> x < y, [11, 12, NULL], [4, 5, 6]) |
+--------------------------------------------------------+
|                                                   NULL |
+--------------------------------------------------------+

select none_match((x,y) -> x < y, [], []);
+-------------------------------------+
| none_match((x, y) -> x < y, [], []) |
+-------------------------------------+
|                                   1 |
+-------------------------------------+

select none_match((x,y) -> x < y, null, [4,5,6]);
+----------------------------------------------+
| none_match((x, y) -> x < y, NULL, [4, 5, 6]) |
+----------------------------------------------+
|                                         NULL |
+----------------------------------------------+
```
