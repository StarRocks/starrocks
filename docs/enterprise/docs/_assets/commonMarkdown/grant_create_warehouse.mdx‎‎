```SQL
GRANT
    { CREATE RESOURCE GROUP | CREATE RESOURCE | CREATE EXTERNAL CATALOG | REPOSITORY | BLACKLIST | FILE | OPERATE | CREATE STORAGE VOLUME | SECURITY | CREATE WAREHOUSE } 
    ON SYSTEM
    TO { ROLE | USER } { <role_name> | <user_identity> } [ WITH GRANT OPTION ]
```
