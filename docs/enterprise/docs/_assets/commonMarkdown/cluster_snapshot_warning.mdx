
:::warning

While the system supports retaining multiple versions of snapshots, restoring the cluster from a particular snapshot invalidates all subsequent snapshot versions, rendering them unusable for future cluster recovery.

:::
