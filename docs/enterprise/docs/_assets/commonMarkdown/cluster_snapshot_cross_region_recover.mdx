1. **(Optional)** If you want to perform a cross-region recovery, or a cross-cluster recovery within the region, you MUST modify the configuration file **cluster_snapshot.yaml** under the directory `fe/conf` of the Leader FE node. Otherwise, you can skip this step.

   To restore the data in a new cluster, all files under the original storage path must be copied to the new path. You must use the corresponding templates.
   - If you want to perform a cross-cluster recovery within the region, use the template of **cluster_snapshot.yaml** in [Appendix - A](#appendix-a).
   - If you want to perform a cross-region recovery, use the template of **cluster_snapshot.yaml** in [Appendix - B](#appendix-b).
