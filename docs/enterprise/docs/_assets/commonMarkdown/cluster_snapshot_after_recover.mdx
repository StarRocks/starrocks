After the new cluster is started, new data will be written in the storage volumes defined in the `storage_volumes` section instead of that in the `snapshot_storage_volume` section. In addition, the original snapshot in `snapshot_storage_volume` will not be deleted or corrupted, allowing for multiple trials.