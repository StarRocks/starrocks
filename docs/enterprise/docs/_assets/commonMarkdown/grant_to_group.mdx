- Grant a role to a user group. You can identify user groups from external authentication systems via [Group Provider](../../administration/user_privs/group_provider.md).

  The following example grants the role `example_role` to the user group `analysts`:

  ```SQL
  GRANT example_role TO EXTERNAL GROUP analysts;
  ```
