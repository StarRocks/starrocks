-- name: test_password_policy
CREATE PASSWORD POLICY pp1 comment "pp1 comment"
properties (
    "PASSWORD_MIN_LENGTH" = "8",
    "PASSWORD_MIN_UPPER_CASE_CHARS" = "1",
    "PASSWORD_MIN_LOWER_CASE_CHARS" = "1",
    "PASSWORD_MIN_NUMERIC_CHARS" = "1",
    "PASSWORD_MIN_SPECIAL_CHARS" = "1",
    "PASSWORD_MAX_AGE_DAYS" = "7"
);
-- result:
-- !result
CREATE PASSWORD POLICY pp1 comment "pp1 comment"
properties (
    "PASSWORD_MIN_LENGTH" = "8",
    "PASSWORD_MIN_UPPER_CASE_CHARS" = "1",
    "PASSWORD_MIN_LOWER_CASE_CHARS" = "1",
    "PASSWORD_MIN_NUMERIC_CHARS" = "1",
    "PASSWORD_MIN_SPECIAL_CHARS" = "1",
    "PASSWORD_MAX_AGE_DAYS" = "7"
);
-- result:
E: (1064, 'Getting analyzing error. Detail message: Password policy pp1 already exists..')
-- !result
show password policies;
-- result:
pp1	pp1 comment	password_min_upper_case_chars = 1, password_max_age_days = 7, password_min_lower_case_chars = 1, password_min_numeric_chars = 1, password_min_length = 8, password_min_special_chars = 1	FALSE
-- !result
show create password policy pp1;
-- result:
pp1	CREATE PASSWORD POLICY pp1 COMMENT "pp1 comment" PROPERTIES ("password_min_lower_case_chars" = "1", "password_min_special_chars" = "1", "password_min_upper_case_chars" = "1", "password_min_length" = "8", "password_max_age_days" = "7", "password_min_numeric_chars" = "1")
-- !result
alter system set password policy pp1;
-- result:
-- !result
drop user if exists u1;
-- result:
-- !result
create user u1 identified by "123";
-- result:
E: (1064, 'Getting analyzing error. Detail message: Does not meet the Password Policy restrictions. Password length cannot be less than 8.')
-- !result
create user u1 identified by "12345678";
-- result:
E: (1064, 'Getting analyzing error. Detail message: Does not meet the Password Policy restrictions. The password contains at least 1 uppercase character.')
-- !result
create user u1 identified by "A2345678";
-- result:
E: (1064, 'Getting analyzing error. Detail message: Does not meet the Password Policy restrictions. The password contains at least 1 lowercase character.')
-- !result
create user u1 identified by "Ab345678";
-- result:
E: (1064, "Getting analyzing error. Detail message: Does not meet the Password Policy restrictions. The password contains at least 1 special character('-', '~', '!', '@', '#', '$', '%', '^', '&', '<', '>', '=', '+').")
-- !result
create user u1 identified by "!Ab345678";
-- result:
-- !result
drop user if exists u1;
-- result:
-- !result
drop password policy pp1;
-- result:
E: (1064, 'Getting analyzing error. Detail message: Cannot delete a password policy that is in use. You can use unset first and then delete it.')
-- !result
alter system unset password policy;
-- result:
-- !result
create user u1 identified by "123";
-- result:
-- !result
alter user u1 identified by "12345678";
-- result:
-- !result
alter user u1 identified by "A2345678";
-- result:
-- !result
alter user u1 identified by "Ab345678";
-- result:
-- !result
alter user u1 identified by "!Ab345678";
-- result:
-- !result
drop password policy pp1;
-- result:
-- !result
drop user u1;
-- result:
-- !result