-- name: test_vector_index_hnsw_quantizer @sequential @native
-- TODO: result file needs to be generated by running this test once locally:
--   cd test && python3 run.py --record test_vector_index/test_vector_index_hnsw_quantizer
-- The recorded -- result: blocks will replace this placeholder.
--
-- Expected behavior:
--   * sq8 / sq4 / pq + m_pq=4 (divides dim=8): CREATE TABLE succeeds, INSERT succeeds,
--     SELECT ... ORDER BY approx_l2_distance ... LIMIT 1 returns id=1
--   * pq without m_pq: CREATE TABLE fails with SemanticException
--   * pq with m_pq=3 (3 does not divide dim=8): CREATE TABLE fails with SemanticException
--   * quantizer="lq2" (unknown): CREATE TABLE fails with SemanticException
