-- name: test_vector_index_hnsw_lazymat @sequential @native
-- TODO: result file needs to be generated by running this test once locally:
--   cd test && python3 run.py --record test_vector_index/test_vector_index_hnsw_lazymat
-- The recorded -- result: blocks will replace this placeholder.
--
-- Expected behavior (deterministic data):
--   * HNSW scenario 1 (SELECT id only):     top-3 = (1, 0.0), (2, 5.0), (3, 20.0)
--   * HNSW scenario 2 (SELECT v, id):       top-2 = (3, [3,3,3,3,3], 0.0),
--                                                    then one of (2, [2,2,2,2,2], 5.0)
--                                                    or (4, [4,4,4,4,4], 5.0)
--   * HNSW scenario 3 (predicate + ANN):    top-3 = (1, 5, 0.0), (2, 5, 5.0), (3, 5, 20.0)
--   * IVFPQ scenario:                        top-2 ≈ (1, 0.0), (2, 4.0) — IVFPQ is approximate;
--                                            recall may be < 100% so the test should accept
--                                            order tolerance once recorded.
