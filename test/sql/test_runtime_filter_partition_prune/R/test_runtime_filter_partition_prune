-- name: test_rf_prune_range_date_broadcast
create database test_rf_prune_${uuid0};
-- result:
-- !result
use test_rf_prune_${uuid0};
-- result:
-- !result
set enable_runtime_filter_partition_prune = true;
-- result:
-- !result
set global_runtime_filter_wait_timeout = 10000;
-- result:
-- !result
CREATE TABLE fact_dt (dt DATE NOT NULL, id INT NOT NULL, v INT) ENGINE=OLAP
DUPLICATE KEY(dt, id)
PARTITION BY RANGE(dt) (
    PARTITION p0105 VALUES [('2024-01-05'), ('2024-01-06')),
    PARTITION p0106 VALUES [('2024-01-06'), ('2024-01-07')),
    PARTITION p0107 VALUES [('2024-01-07'), ('2024-01-08')),
    PARTITION p0108 VALUES [('2024-01-08'), ('2024-01-09')),
    PARTITION p0109 VALUES [('2024-01-09'), ('2024-01-10'))
)
DISTRIBUTED BY HASH(id) BUCKETS 2
PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO fact_dt VALUES
    ('2024-01-05', 1, 10), ('2024-01-05', 2, 11),
    ('2024-01-06', 3, 20), ('2024-01-06', 4, 21),
    ('2024-01-07', 5, 30), ('2024-01-07', 6, 31),
    ('2024-01-08', 7, 40), ('2024-01-08', 8, 41),
    ('2024-01-09', 9, 50), ('2024-01-09', 10, 51);
-- result:
-- !result
CREATE TABLE dim_dt (dt DATE NOT NULL, name VARCHAR(16)) ENGINE=OLAP
DUPLICATE KEY(dt) DISTRIBUTED BY HASH(dt) BUCKETS 1 PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO dim_dt VALUES ('2024-01-07', 'hit');
-- result:
-- !result
SELECT f.dt, f.id, f.v FROM fact_dt f JOIN [broadcast] dim_dt d ON f.dt = d.dt ORDER BY f.id;
-- result:
2024-01-07	5	30
2024-01-07	6	31
-- !result
TRUNCATE TABLE dim_dt;
-- result:
-- !result
INSERT INTO dim_dt VALUES ('2024-01-05', 'lower'), ('2024-01-09', 'lower');
-- result:
-- !result
SELECT f.dt, f.id FROM fact_dt f JOIN [broadcast] dim_dt d ON f.dt = d.dt ORDER BY f.id;
-- result:
2024-01-05	1
2024-01-05	2
2024-01-09	9
2024-01-09	10
-- !result
TRUNCATE TABLE dim_dt;
-- result:
-- !result
INSERT INTO dim_dt VALUES ('2024-03-01', 'miss');
-- result:
-- !result
SELECT count(*), ifnull(sum(f.v), 0) FROM fact_dt f JOIN [broadcast] dim_dt d ON f.dt = d.dt;
-- result:
0	0
-- !result
TRUNCATE TABLE dim_dt;
-- result:
-- !result
INSERT INTO dim_dt VALUES ('2024-01-05', 'a'), ('2024-01-06', 'b'), ('2024-01-07', 'c'), ('2024-01-08', 'd'), ('2024-01-09', 'e');
-- result:
-- !result
SELECT count(*), sum(f.v) FROM fact_dt f JOIN [broadcast] dim_dt d ON f.dt = d.dt;
-- result:
10	305
-- !result
set enable_runtime_filter_partition_prune = false;
-- result:
-- !result
TRUNCATE TABLE dim_dt;
-- result:
-- !result
INSERT INTO dim_dt VALUES ('2024-01-07', 'c');
-- result:
-- !result
SELECT f.dt, f.id, f.v FROM fact_dt f JOIN [broadcast] dim_dt d ON f.dt = d.dt ORDER BY f.id;
-- result:
2024-01-07	5	30
2024-01-07	6	31
-- !result
drop database test_rf_prune_${uuid0} force;
-- result:
-- !result

-- name: test_rf_prune_range_int_boundaries
create database test_rf_prune_${uuid0};
-- result:
-- !result
use test_rf_prune_${uuid0};
-- result:
-- !result
set enable_runtime_filter_partition_prune = true;
-- result:
-- !result
set global_runtime_filter_wait_timeout = 10000;
-- result:
-- !result
CREATE TABLE fact_k (k INT NOT NULL, id INT NOT NULL, v INT) ENGINE=OLAP
DUPLICATE KEY(k, id)
PARTITION BY RANGE(k) (
    PARTITION pneg VALUES [('-100'), ('0')),
    PARTITION p0 VALUES [('0'), ('100')),
    PARTITION p1 VALUES [('100'), ('200'))
)
DISTRIBUTED BY HASH(id) BUCKETS 1
PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO fact_k VALUES (-100, 1, 1), (-1, 2, 2), (0, 3, 3), (99, 4, 4), (100, 5, 5), (199, 6, 6);
-- result:
-- !result
CREATE TABLE dim_k (k INT NOT NULL) ENGINE=OLAP DUPLICATE KEY(k) DISTRIBUTED BY HASH(k) BUCKETS 1 PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO dim_k VALUES (-100), (0), (99), (100);
-- result:
-- !result
SELECT f.k, f.id FROM fact_k f JOIN [broadcast] dim_k d ON f.k = d.k ORDER BY f.id;
-- result:
-100	1
0	3
99	4
100	5
-- !result
TRUNCATE TABLE dim_k;
-- result:
-- !result
INSERT INTO dim_k VALUES (-101), (200);
-- result:
-- !result
SELECT count(*) FROM fact_k f JOIN [broadcast] dim_k d ON f.k = d.k;
-- result:
0
-- !result
TRUNCATE TABLE dim_k;
-- result:
-- !result
INSERT INTO dim_k VALUES (-1), (199);
-- result:
-- !result
SELECT f.k, f.id FROM fact_k f JOIN [broadcast] dim_k d ON f.k = d.k ORDER BY f.id;
-- result:
-1	2
199	6
-- !result
drop database test_rf_prune_${uuid0} force;
-- result:
-- !result

-- name: test_rf_prune_list_string
create database test_rf_prune_${uuid0};
-- result:
-- !result
use test_rf_prune_${uuid0};
-- result:
-- !result
set enable_runtime_filter_partition_prune = true;
-- result:
-- !result
-- a low-cardinality dictionary would rewrite the probe slot to an integer, which the pruner skips
set cbo_enable_low_cardinality_optimize = false;
-- result:
-- !result
set global_runtime_filter_wait_timeout = 10000;
-- result:
-- !result
CREATE TABLE fact_city (city VARCHAR(16), id INT NOT NULL, v INT) ENGINE=OLAP
DUPLICATE KEY(city, id)
PARTITION BY LIST(city) (
    PARTITION pbj VALUES IN ('beijing'),
    PARTITION psh VALUES IN ('shanghai', 'shenzhen', 'guangzhou'),
    PARTITION pnull VALUES IN (NULL)
)
DISTRIBUTED BY HASH(id) BUCKETS 1
PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO fact_city VALUES ('beijing', 1, 1), ('shanghai', 2, 2), ('shenzhen', 3, 3), ('guangzhou', 4, 4), (NULL, 5, 5);
-- result:
-- !result
CREATE TABLE dim_city (city VARCHAR(16)) ENGINE=OLAP DUPLICATE KEY(city) DISTRIBUTED BY HASH(city) BUCKETS 1 PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO dim_city VALUES ('shenzhen');
-- result:
-- !result
SELECT f.city, f.id FROM fact_city f JOIN [broadcast] dim_city d ON f.city = d.city ORDER BY f.id;
-- result:
shenzhen	3
-- !result
INSERT INTO dim_city VALUES (NULL);
-- result:
-- !result
SELECT f.city, f.id FROM fact_city f JOIN [broadcast] dim_city d ON f.city <=> d.city ORDER BY f.id;
-- result:
shenzhen	3
None	5
-- !result
SELECT f.city, f.id FROM fact_city f JOIN [broadcast] dim_city d ON f.city = d.city ORDER BY f.id;
-- result:
shenzhen	3
-- !result
CREATE TABLE fact_code (code CHAR(8) NOT NULL, id INT NOT NULL, v INT) ENGINE=OLAP
DUPLICATE KEY(code, id)
PARTITION BY LIST(code) (
    PARTITION pa VALUES IN ('AA'),
    PARTITION pb VALUES IN ('BB'),
    PARTITION pc VALUES IN ('CC')
)
DISTRIBUTED BY HASH(id) BUCKETS 1
PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO fact_code VALUES ('AA', 1, 1), ('BB', 2, 2), ('CC', 3, 3);
-- result:
-- !result
CREATE TABLE dim_code (code CHAR(8) NOT NULL) ENGINE=OLAP DUPLICATE KEY(code) DISTRIBUTED BY HASH(code) BUCKETS 1 PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO dim_code VALUES ('BB');
-- result:
-- !result
SELECT f.code, f.id FROM fact_code f JOIN [broadcast] dim_code d ON f.code = d.code ORDER BY f.id;
-- result:
BB	2
-- !result
CREATE TABLE fact_mix (city VARCHAR(16), id INT NOT NULL) ENGINE=OLAP
DUPLICATE KEY(city, id)
PARTITION BY LIST(city) (
    PARTITION pmix VALUES IN ('guangzhou', NULL),
    PARTITION psh VALUES IN ('shanghai')
)
DISTRIBUTED BY HASH(id) BUCKETS 1
PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO fact_mix VALUES ('guangzhou', 1), (NULL, 2), ('shanghai', 3);
-- result:
-- !result
TRUNCATE TABLE dim_city;
-- result:
-- !result
INSERT INTO dim_city VALUES ('guangzhou');
-- result:
-- !result
SELECT f.city, f.id FROM fact_mix f JOIN [broadcast] dim_city d ON f.city = d.city ORDER BY f.id;
-- result:
guangzhou	1
-- !result
TRUNCATE TABLE dim_city;
-- result:
-- !result
INSERT INTO dim_city VALUES ('shanghai');
-- result:
-- !result
SELECT f.city, f.id FROM fact_mix f JOIN [broadcast] dim_city d ON f.city = d.city ORDER BY f.id;
-- result:
shanghai	3
-- !result
drop database test_rf_prune_${uuid0} force;
-- result:
-- !result

-- name: test_rf_prune_bloom_only_shuffle
create database test_rf_prune_${uuid0};
-- result:
-- !result
use test_rf_prune_${uuid0};
-- result:
-- !result
set enable_runtime_filter_partition_prune = true;
-- result:
-- !result
-- a low-cardinality dictionary would rewrite the probe slot to an integer, which the pruner skips
set cbo_enable_low_cardinality_optimize = false;
-- result:
-- !result
set global_runtime_filter_wait_timeout = 10000;
-- result:
-- !result
CREATE TABLE fact_dt (dt DATE NOT NULL, id INT NOT NULL, v INT) ENGINE=OLAP
DUPLICATE KEY(dt, id)
PARTITION BY RANGE(dt) (
    PARTITION p0105 VALUES [('2024-01-05'), ('2024-01-06')),
    PARTITION p0106 VALUES [('2024-01-06'), ('2024-01-07')),
    PARTITION p0107 VALUES [('2024-01-07'), ('2024-01-08'))
)
DISTRIBUTED BY HASH(id) BUCKETS 2
PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO fact_dt VALUES ('2024-01-05', 1, 10), ('2024-01-06', 2, 20), ('2024-01-07', 3, 30);
-- result:
-- !result
CREATE TABLE dim_dt (dt DATE NOT NULL, name VARCHAR(16)) ENGINE=OLAP DUPLICATE KEY(dt) DISTRIBUTED BY HASH(dt) BUCKETS 1 PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO dim_dt VALUES ('2024-01-06', 'hit');
-- result:
-- !result
SELECT f.dt, f.id, f.v FROM fact_dt f JOIN [shuffle] dim_dt d ON f.dt = d.dt ORDER BY f.id;
-- result:
2024-01-06	2	20
-- !result
CREATE TABLE fact_city (city VARCHAR(16) NOT NULL, id INT NOT NULL) ENGINE=OLAP
DUPLICATE KEY(city, id)
PARTITION BY LIST(city) (
    PARTITION pbj VALUES IN ('beijing'),
    PARTITION psh VALUES IN ('shanghai'),
    PARTITION pgz VALUES IN ('guangzhou')
)
DISTRIBUTED BY HASH(id) BUCKETS 2
PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO fact_city VALUES ('beijing', 1), ('shanghai', 2), ('guangzhou', 3);
-- result:
-- !result
CREATE TABLE dim_city (city VARCHAR(16) NOT NULL) ENGINE=OLAP DUPLICATE KEY(city) DISTRIBUTED BY HASH(city) BUCKETS 1 PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO dim_city VALUES ('guangzhou');
-- result:
-- !result
SELECT f.city, f.id FROM fact_city f JOIN [shuffle] dim_city d ON f.city = d.city ORDER BY f.id;
-- result:
guangzhou	3
-- !result
drop database test_rf_prune_${uuid0} force;
-- result:
-- !result

-- name: test_rf_prune_multi_column_range
create database test_rf_prune_${uuid0};
-- result:
-- !result
use test_rf_prune_${uuid0};
-- result:
-- !result
set enable_runtime_filter_partition_prune = true;
-- result:
-- !result
set global_runtime_filter_wait_timeout = 10000;
-- result:
-- !result
CREATE TABLE fact_ab (a INT NOT NULL, b INT NOT NULL, id INT NOT NULL) ENGINE=OLAP
DUPLICATE KEY(a, b, id)
PARTITION BY RANGE(a, b) (
    PARTITION p1 VALUES [('1', '100'), ('5', '50')),
    PARTITION p2 VALUES [('5', '50'), ('9', '0'))
)
DISTRIBUTED BY HASH(id) BUCKETS 1
PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO fact_ab VALUES (2, 0, 1), (5, 10, 2), (5, 60, 3), (7, 0, 4);
-- result:
-- !result
CREATE TABLE dim_a (a INT NOT NULL) ENGINE=OLAP DUPLICATE KEY(a) DISTRIBUTED BY HASH(a) BUCKETS 1 PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO dim_a VALUES (5);
-- result:
-- !result
SELECT f.a, f.b, f.id FROM fact_ab f JOIN [broadcast] dim_a d ON f.a = d.a ORDER BY f.id;
-- result:
5	10	2
5	60	3
-- !result
TRUNCATE TABLE dim_a;
-- result:
-- !result
INSERT INTO dim_a VALUES (7);
-- result:
-- !result
SELECT f.a, f.b, f.id FROM fact_ab f JOIN [broadcast] dim_a d ON f.a = d.a ORDER BY f.id;
-- result:
7	0	4
-- !result
drop database test_rf_prune_${uuid0} force;
-- result:
-- !result

-- name: test_rf_prune_pipeline_dop
create database test_rf_prune_${uuid0};
-- result:
-- !result
use test_rf_prune_${uuid0};
-- result:
-- !result
set enable_runtime_filter_partition_prune = true;
-- result:
-- !result
set global_runtime_filter_wait_timeout = 10000;
-- result:
-- !result
CREATE TABLE fact_dt (dt DATE NOT NULL, id INT NOT NULL, v INT) ENGINE=OLAP
DUPLICATE KEY(dt, id)
PARTITION BY RANGE(dt) (
    PARTITION p0105 VALUES [('2024-01-05'), ('2024-01-06')),
    PARTITION p0106 VALUES [('2024-01-06'), ('2024-01-07')),
    PARTITION p0107 VALUES [('2024-01-07'), ('2024-01-08'))
)
DISTRIBUTED BY HASH(id) BUCKETS 4
PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO fact_dt VALUES ('2024-01-05', 1, 10), ('2024-01-05', 2, 11), ('2024-01-06', 3, 20), ('2024-01-06', 4, 21), ('2024-01-07', 5, 30), ('2024-01-07', 6, 31);
-- result:
-- !result
CREATE TABLE dim_dt (dt DATE NOT NULL) ENGINE=OLAP DUPLICATE KEY(dt) DISTRIBUTED BY HASH(dt) BUCKETS 1 PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO dim_dt VALUES ('2024-01-06');
-- result:
-- !result
set pipeline_dop = 1;
-- result:
-- !result
SELECT f.dt, f.id, f.v FROM fact_dt f JOIN [broadcast] dim_dt d ON f.dt = d.dt ORDER BY f.id;
-- result:
2024-01-06	3	20
2024-01-06	4	21
-- !result
set pipeline_dop = 4;
-- result:
-- !result
SELECT f.dt, f.id, f.v FROM fact_dt f JOIN [broadcast] dim_dt d ON f.dt = d.dt ORDER BY f.id;
-- result:
2024-01-06	3	20
2024-01-06	4	21
-- !result
drop database test_rf_prune_${uuid0} force;
-- result:
-- !result

-- name: test_rf_prune_not_applicable
create database test_rf_prune_${uuid0};
-- result:
-- !result
use test_rf_prune_${uuid0};
-- result:
-- !result
set enable_runtime_filter_partition_prune = true;
-- result:
-- !result
set global_runtime_filter_wait_timeout = 10000;
-- result:
-- !result
CREATE TABLE fact_dt (dt DATE NOT NULL, id INT NOT NULL, v INT) ENGINE=OLAP
DUPLICATE KEY(dt, id)
PARTITION BY RANGE(dt) (
    PARTITION p0105 VALUES [('2024-01-05'), ('2024-01-06')),
    PARTITION p0106 VALUES [('2024-01-06'), ('2024-01-07'))
)
DISTRIBUTED BY HASH(id) BUCKETS 1
PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO fact_dt VALUES ('2024-01-05', 1, 10), ('2024-01-06', 2, 20);
-- result:
-- !result
CREATE TABLE dim_misc (id INT NOT NULL, ts DATETIME NOT NULL) ENGINE=OLAP DUPLICATE KEY(id) DISTRIBUTED BY HASH(id) BUCKETS 1 PROPERTIES ("replication_num" = "1");
-- result:
-- !result
INSERT INTO dim_misc VALUES (2, '2024-01-06 00:00:00');
-- result:
-- !result
SELECT f.dt, f.id FROM fact_dt f JOIN [broadcast] dim_misc d ON f.id = d.id ORDER BY f.id;
-- result:
2024-01-06	2
-- !result
SELECT f.dt, f.id FROM fact_dt f JOIN [broadcast] dim_misc d ON cast(f.dt as datetime) = d.ts ORDER BY f.id;
-- result:
2024-01-06	2
-- !result
drop database test_rf_prune_${uuid0} force;
-- result:
-- !result
-- name: test_rf_prune_list_compact_values
create database test_rf_prune_${uuid0};
-- result:
-- !result
use test_rf_prune_${uuid0};
-- result:
-- !result
set global_runtime_filter_wait_timeout = 10000;
-- result:
-- !result
CREATE TABLE fact_num (k BIGINT, id INT NOT NULL) DUPLICATE KEY(k, id)
PARTITION BY LIST(k) (PARTITION p0 VALUES IN ('-1', '1'),
PARTITION p1 VALUES IN ('9223372036854775806', '9223372036854775807'), PARTITION pn VALUES IN (NULL))
DISTRIBUTED BY HASH(id) BUCKETS 1 PROPERTIES ("replication_num"="1");
-- result:
-- !result
INSERT INTO fact_num VALUES ('-1',1), ('1',2), ('9223372036854775806',3), ('9223372036854775807',4), (NULL,5);
-- result:
-- !result
CREATE TABLE dim_num (k BIGINT) DUPLICATE KEY(k) DISTRIBUTED BY HASH(k) BUCKETS 1
PROPERTIES ("replication_num"="1");
-- result:
-- !result
INSERT INTO dim_num VALUES ('1'), (NULL);
-- result:
-- !result
set enable_runtime_filter_partition_prune = false;
-- result:
-- !result
SELECT f.id FROM fact_num f JOIN [broadcast] dim_num d ON f.k = d.k ORDER BY f.id;
-- result:
2
-- !result
SELECT f.id FROM fact_num f JOIN [broadcast] dim_num d ON f.k <=> d.k ORDER BY f.id;
-- result:
2
5
-- !result
SELECT f.id FROM fact_num f JOIN [shuffle] dim_num d ON f.k <=> d.k ORDER BY f.id;
-- result:
2
5
-- !result
set enable_runtime_filter_partition_prune = true;
-- result:
-- !result
SELECT f.id FROM fact_num f JOIN [broadcast] dim_num d ON f.k = d.k ORDER BY f.id;
-- result:
2
-- !result
SELECT f.id FROM fact_num f JOIN [broadcast] dim_num d ON f.k <=> d.k ORDER BY f.id;
-- result:
2
5
-- !result
SELECT f.id FROM fact_num f JOIN [shuffle] dim_num d ON f.k <=> d.k ORDER BY f.id;
-- result:
2
5
-- !result
TRUNCATE TABLE dim_num;
-- result:
-- !result
INSERT INTO dim_num VALUES ('42');
-- result:
-- !result
set enable_runtime_filter_partition_prune = false;
-- result:
-- !result
SELECT count(*) FROM fact_num f JOIN [shuffle] dim_num d ON f.k = d.k;
-- result:
0
-- !result
set enable_runtime_filter_partition_prune = true;
-- result:
-- !result
SELECT count(*) FROM fact_num f JOIN [shuffle] dim_num d ON f.k = d.k;
-- result:
0
-- !result
TRUNCATE TABLE dim_num;
-- result:
-- !result
INSERT INTO dim_num VALUES ('-1'), ('1'), ('9223372036854775806'), ('9223372036854775807'), (NULL);
-- result:
-- !result
set enable_runtime_filter_partition_prune = false;
-- result:
-- !result
SELECT count(*), sum(f.id) FROM fact_num f JOIN [broadcast] dim_num d ON f.k <=> d.k;
-- result:
5	15
-- !result
set enable_runtime_filter_partition_prune = true;
-- result:
-- !result
SELECT count(*), sum(f.id) FROM fact_num f JOIN [broadcast] dim_num d ON f.k <=> d.k;
-- result:
5	15
-- !result
CREATE TABLE fact_date (k DATE, id INT NOT NULL) DUPLICATE KEY(k, id)
PARTITION BY LIST(k) (PARTITION p0 VALUES IN ('2024-02-28', '2024-02-29'),
PARTITION p1 VALUES IN ('2024-03-01', '2024-03-02'), PARTITION pn VALUES IN (NULL))
DISTRIBUTED BY HASH(id) BUCKETS 1 PROPERTIES ("replication_num"="1");
-- result:
-- !result
INSERT INTO fact_date VALUES ('2024-02-28',1), ('2024-02-29',2), ('2024-03-01',3), ('2024-03-02',4), (NULL,5);
-- result:
-- !result
CREATE TABLE dim_date (k DATE) DUPLICATE KEY(k) DISTRIBUTED BY HASH(k) BUCKETS 1
PROPERTIES ("replication_num"="1");
-- result:
-- !result
INSERT INTO dim_date VALUES ('2024-02-29'), (NULL);
-- result:
-- !result
set enable_runtime_filter_partition_prune = false;
-- result:
-- !result
SELECT f.id FROM fact_date f JOIN [broadcast] dim_date d ON f.k = d.k ORDER BY f.id;
-- result:
2
-- !result
SELECT f.id FROM fact_date f JOIN [broadcast] dim_date d ON f.k <=> d.k ORDER BY f.id;
-- result:
2
5
-- !result
SELECT f.id FROM fact_date f JOIN [shuffle] dim_date d ON f.k <=> d.k ORDER BY f.id;
-- result:
2
5
-- !result
set enable_runtime_filter_partition_prune = true;
-- result:
-- !result
SELECT f.id FROM fact_date f JOIN [broadcast] dim_date d ON f.k = d.k ORDER BY f.id;
-- result:
2
-- !result
SELECT f.id FROM fact_date f JOIN [broadcast] dim_date d ON f.k <=> d.k ORDER BY f.id;
-- result:
2
5
-- !result
SELECT f.id FROM fact_date f JOIN [shuffle] dim_date d ON f.k <=> d.k ORDER BY f.id;
-- result:
2
5
-- !result
TRUNCATE TABLE dim_date;
-- result:
-- !result
INSERT INTO dim_date VALUES ('2025-01-01');
-- result:
-- !result
set enable_runtime_filter_partition_prune = false;
-- result:
-- !result
SELECT count(*) FROM fact_date f JOIN [shuffle] dim_date d ON f.k = d.k;
-- result:
0
-- !result
set enable_runtime_filter_partition_prune = true;
-- result:
-- !result
SELECT count(*) FROM fact_date f JOIN [shuffle] dim_date d ON f.k = d.k;
-- result:
0
-- !result
TRUNCATE TABLE dim_date;
-- result:
-- !result
INSERT INTO dim_date VALUES ('2024-02-28'), ('2024-02-29'), ('2024-03-01'), ('2024-03-02'), (NULL);
-- result:
-- !result
set enable_runtime_filter_partition_prune = false;
-- result:
-- !result
SELECT count(*), sum(f.id) FROM fact_date f JOIN [broadcast] dim_date d ON f.k <=> d.k;
-- result:
5	15
-- !result
set enable_runtime_filter_partition_prune = true;
-- result:
-- !result
SELECT count(*), sum(f.id) FROM fact_date f JOIN [broadcast] dim_date d ON f.k <=> d.k;
-- result:
5	15
-- !result
drop database test_rf_prune_${uuid0} force;
-- result:
-- !result
-- name: test_rf_prune_multi_column_list
create database test_rf_prune_${uuid0};
-- result:
-- !result
use test_rf_prune_${uuid0};
-- result:
-- !result
set cbo_enable_low_cardinality_optimize = false;
-- result:
-- !result
set global_runtime_filter_wait_timeout = 10000;
-- result:
-- !result
CREATE TABLE fact (tenant INT, city VARCHAR(16), id INT NOT NULL) DUPLICATE KEY(tenant, city, id)
PARTITION BY LIST(tenant, city) (PARTITION p0 VALUES IN (('1','a'),('1','b'),('2','a'),(NULL,'a')),
PARTITION p1 VALUES IN (('3','a'),('3','c'),('4','c')),
PARTITION p2 VALUES IN (('5','b'),(NULL,'b')))
DISTRIBUTED BY HASH(id) BUCKETS 1 PROPERTIES ("replication_num"="1");
-- result:
-- !result
INSERT INTO fact VALUES (1,'a',1),(1,'b',2),(2,'a',3),(NULL,'a',4),
(3,'a',5),(3,'c',6),(4,'c',7),(5,'b',8),(NULL,'b',9);
-- result:
-- !result
CREATE TABLE dim (tenant INT, city VARCHAR(16)) DUPLICATE KEY(tenant, city)
DISTRIBUTED BY HASH(tenant) BUCKETS 1 PROPERTIES ("replication_num"="1");
-- result:
-- !result
INSERT INTO dim VALUES (1,'a'),(3,'c'),(NULL,'a');
-- result:
-- !result
set enable_runtime_filter_partition_prune = false;
-- result:
-- !result
SELECT f.id FROM fact f JOIN [broadcast] dim d ON f.tenant = d.tenant ORDER BY f.id;
-- result:
1
2
5
6
-- !result
SELECT f.id FROM fact f JOIN [broadcast] dim d ON f.tenant <=> d.tenant ORDER BY f.id;
-- result:
1
2
4
5
6
9
-- !result
SELECT f.id FROM fact f JOIN [broadcast] dim d ON f.city = d.city ORDER BY f.id;
-- result:
1
1
3
3
4
4
5
5
6
7
-- !result
SELECT f.id FROM fact f JOIN [broadcast] dim d ON f.tenant <=> d.tenant AND f.city = d.city ORDER BY f.id;
-- result:
1
4
6
-- !result
set enable_runtime_filter_partition_prune = true;
-- result:
-- !result
SELECT f.id FROM fact f JOIN [broadcast] dim d ON f.tenant = d.tenant ORDER BY f.id;
-- result:
1
2
5
6
-- !result
SELECT f.id FROM fact f JOIN [broadcast] dim d ON f.tenant <=> d.tenant ORDER BY f.id;
-- result:
1
2
4
5
6
9
-- !result
SELECT f.id FROM fact f JOIN [broadcast] dim d ON f.city = d.city ORDER BY f.id;
-- result:
1
1
3
3
4
4
5
5
6
7
-- !result
SELECT f.id FROM fact f JOIN [broadcast] dim d ON f.tenant <=> d.tenant AND f.city = d.city ORDER BY f.id;
-- result:
1
4
6
-- !result
set dynamic_partition_prune_limit = 1;
-- result:
-- !result
SELECT f.id FROM fact f JOIN [broadcast] dim d ON f.tenant <=> d.tenant AND f.city = d.city ORDER BY f.id;
-- result:
1
4
6
-- !result
drop database test_rf_prune_${uuid0} force;
-- result:
-- !result