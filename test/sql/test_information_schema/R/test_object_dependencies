-- name: test_object_dependencies
create database db_${uuid0};
-- result:
-- !result
use db_${uuid0};
-- result:
-- !result
create table t1 (c1 int, c2 string);
-- result:
-- !result
create materialized view mv1 refresh async as select * from t1;
-- result:
-- !result
select 
    object_name,
    object_catalog,
    object_type,
    ref_object_name,
    ref_object_catalog,
    ref_object_type
from sys.object_dependencies where object_database = 'db_${uuid0}'
order by object_name, ref_object_name;
-- result:
[REGEX]mv1\tdefault_catalog\t(CLOUD_NATIVE_)?MATERIALIZED_VIEW\tt1\tdefault_catalog\t(OLAP|CLOUD_NATIVE)
-- !result
create view v1 as select * from t1;
-- result:
-- !result
create materialized view mv2 refresh async as select * from t1;
-- result:
-- !result
select 
    object_name,
    object_catalog,
    object_type,
    ref_object_name,
    ref_object_catalog,
    ref_object_type
from sys.object_dependencies where object_database = 'db_${uuid0}'
order by object_name, ref_object_name;
-- result:
[REGEX](?s)mv1\tdefault_catalog\t(CLOUD_NATIVE_)?MATERIALIZED_VIEW\tt1\tdefault_catalog\t(OLAP|CLOUD_NATIVE)\nmv2\tdefault_catalog\t(CLOUD_NATIVE_)?MATERIALIZED_VIEW\tt1\tdefault_catalog\t(OLAP|CLOUD_NATIVE)
-- !result
create materialized view mv3 refresh async as select * from mv2;
-- result:
-- !result
select 
    object_name,
    object_catalog,
    object_type,
    ref_object_name,
    ref_object_catalog,
    ref_object_type
from sys.object_dependencies where object_database = 'db_${uuid0}'
order by object_name, ref_object_name;
-- result:
[REGEX](?s)mv1\tdefault_catalog\t(CLOUD_NATIVE_)?MATERIALIZED_VIEW\tt1\tdefault_catalog\t(OLAP|CLOUD_NATIVE)\nmv2\tdefault_catalog\t(CLOUD_NATIVE_)?MATERIALIZED_VIEW\tt1\tdefault_catalog\t(OLAP|CLOUD_NATIVE)\nmv3\tdefault_catalog\t(CLOUD_NATIVE_)?MATERIALIZED_VIEW\tmv2\tdefault_catalog\t(CLOUD_NATIVE_)?MATERIALIZED_VIEW
-- !result
alter table t1 rename t2;
-- result:
-- !result
select object_name, object_catalog, object_type, ref_object_name, ref_object_catalog, ref_object_type 
from sys.object_dependencies where object_database = 'db_${uuid0}' and object_name = 'mv1'
order by object_name, ref_object_name;
-- result:
[REGEX]mv1\tdefault_catalog\t(CLOUD_NATIVE_)?MATERIALIZED_VIEW\tt2\tdefault_catalog\t(OLAP|CLOUD_NATIVE)
-- !result
create table t1 like t2;
-- result:
-- !result
alter table t1 swap with t2;
-- result:
-- !result
select object_name, object_catalog, object_type, ref_object_name, ref_object_catalog, ref_object_type 
from sys.object_dependencies where object_database = 'db_${uuid0}' and object_name = 'mv1'
order by object_name, ref_object_name;
-- result:
[REGEX]mv1\tdefault_catalog\t(CLOUD_NATIVE_)?MATERIALIZED_VIEW\tt1\tdefault_catalog\t(OLAP|CLOUD_NATIVE)
-- !result
drop table t1;
-- result:
-- !result
select object_name, object_catalog, object_type, ref_object_name, ref_object_catalog, ref_object_type 
from sys.object_dependencies where object_database = 'db_${uuid0}' and object_name = 'mv1'
order by object_name, ref_object_name;
-- result:
[REGEX]mv1\tdefault_catalog\t(CLOUD_NATIVE_)?MATERIALIZED_VIEW\tt1\tdefault_catalog\tUNKNOWN
-- !result
alter table t2 rename t1;
-- result:
-- !result
alter materialized view mv1 active;
-- result:
-- !result
refresh materialized view mv1 with sync mode;
select object_name, object_catalog, object_type, ref_object_name, ref_object_catalog, ref_object_type 
from sys.object_dependencies where object_database = 'db_${uuid0}' and object_name = 'mv1'
order by object_name, ref_object_name;
-- result:
[REGEX]mv1\tdefault_catalog\t(CLOUD_NATIVE_)?MATERIALIZED_VIEW\tt1\tdefault_catalog\t(OLAP|CLOUD_NATIVE)
-- !result
drop database db_${uuid0};
-- result:
-- !result
