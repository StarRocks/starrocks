-- name: test_adbc_create_and_show
set catalog default_catalog;
-- result:
-- !result
create external catalog adbc_catalog_${uuid0}
properties
(
    "type"="adbc",
    "adbc.driver"="flight_sql",
    "adbc.url"="grpc://${external_flightsql_ip}:${external_flightsql_port}"
);
-- result:
-- !result
set catalog adbc_catalog_${uuid0};
-- result:
-- !result
show databases;
-- result:
test_db
-- !result
use test_db;
-- result:
-- !result
show tables;
-- result:
test_join
test_types
-- !result
desc test_types;
-- result:
id	int	YES	true	None
name	varchar(65533)	YES	true	None
value	double	YES	true	None
created_date	date	YES	true	None
flag	boolean	YES	true	None
-- !result
set catalog default_catalog;
-- result:
-- !result
drop catalog adbc_catalog_${uuid0};
-- result:
-- !result

-- name: test_adbc_alter_catalog
set catalog default_catalog;
-- result:
-- !result
create external catalog adbc_catalog_${uuid0}
properties
(
    "type"="adbc",
    "adbc.driver"="flight_sql",
    "adbc.url"="grpc://${external_flightsql_ip}:${external_flightsql_port}"
);
-- result:
-- !result
alter catalog adbc_catalog_${uuid0} set ("adbc.identifier.quote"="`");
-- result:
-- !result
set catalog adbc_catalog_${uuid0};
-- result:
-- !result
show databases;
-- result:
test_db
-- !result
set catalog default_catalog;
-- result:
-- !result
drop catalog adbc_catalog_${uuid0};
-- result:
-- !result

-- name: test_adbc_drop_catalog
set catalog default_catalog;
-- result:
-- !result
create external catalog adbc_catalog_${uuid0}
properties
(
    "type"="adbc",
    "adbc.driver"="flight_sql",
    "adbc.url"="grpc://${external_flightsql_ip}:${external_flightsql_port}"
);
-- result:
-- !result
[UC]show catalogs;
-- result:
-- !result
drop catalog adbc_catalog_${uuid0};
-- result:
-- !result
[UC]show catalogs;
-- result:
-- !result
