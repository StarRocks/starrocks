-- name: test_adbc_select_all
set catalog default_catalog;
-- result:
-- !result
create external catalog adbc_catalog_${uuid0}
properties
(
    "type"="adbc",
    "adbc.driver"="flight_sql",
    "adbc.url"="grpc://${external_flightsql_ip}:${external_flightsql_port}"
);
-- result:
-- !result
set catalog adbc_catalog_${uuid0};
-- result:
-- !result
select * from test_db.test_types order by id;
-- result:
1	alpha	1.5	2024-01-01	1
2	beta	2.5	2024-01-02	0
3	gamma	3.5	2024-01-03	1
4	delta	4.5	2024-01-04	0
5	epsilon	5.5	2024-01-05	1
-- !result
set catalog default_catalog;
-- result:
-- !result
drop catalog adbc_catalog_${uuid0};
-- result:
-- !result

-- name: test_adbc_column_pruning
set catalog default_catalog;
-- result:
-- !result
create external catalog adbc_catalog_${uuid0}
properties
(
    "type"="adbc",
    "adbc.driver"="flight_sql",
    "adbc.url"="grpc://${external_flightsql_ip}:${external_flightsql_port}"
);
-- result:
-- !result
set catalog adbc_catalog_${uuid0};
-- result:
-- !result
select id, name from test_db.test_types order by id;
-- result:
1	alpha
2	beta
3	gamma
4	delta
5	epsilon
-- !result
set catalog default_catalog;
-- result:
-- !result
drop catalog adbc_catalog_${uuid0};
-- result:
-- !result

-- name: test_adbc_predicate_pushdown
set catalog default_catalog;
-- result:
-- !result
create external catalog adbc_catalog_${uuid0}
properties
(
    "type"="adbc",
    "adbc.driver"="flight_sql",
    "adbc.url"="grpc://${external_flightsql_ip}:${external_flightsql_port}"
);
-- result:
-- !result
set catalog adbc_catalog_${uuid0};
-- result:
-- !result
select name from test_db.test_types where id > 3 order by name;
-- result:
delta
epsilon
-- !result
set catalog default_catalog;
-- result:
-- !result
drop catalog adbc_catalog_${uuid0};
-- result:
-- !result

-- name: test_adbc_limit_pushdown
set catalog default_catalog;
-- result:
-- !result
create external catalog adbc_catalog_${uuid0}
properties
(
    "type"="adbc",
    "adbc.driver"="flight_sql",
    "adbc.url"="grpc://${external_flightsql_ip}:${external_flightsql_port}"
);
-- result:
-- !result
set catalog adbc_catalog_${uuid0};
-- result:
-- !result
select name from test_db.test_types order by id limit 2;
-- result:
alpha
beta
-- !result
set catalog default_catalog;
-- result:
-- !result
drop catalog adbc_catalog_${uuid0};
-- result:
-- !result

-- name: test_adbc_combined_pushdown
set catalog default_catalog;
-- result:
-- !result
create external catalog adbc_catalog_${uuid0}
properties
(
    "type"="adbc",
    "adbc.driver"="flight_sql",
    "adbc.url"="grpc://${external_flightsql_ip}:${external_flightsql_port}"
);
-- result:
-- !result
set catalog adbc_catalog_${uuid0};
-- result:
-- !result
select name from test_db.test_types where flag = true order by id limit 2;
-- result:
alpha
gamma
-- !result
set catalog default_catalog;
-- result:
-- !result
drop catalog adbc_catalog_${uuid0};
-- result:
-- !result

-- name: test_adbc_explain_pushdown
set catalog default_catalog;
-- result:
-- !result
create external catalog adbc_catalog_${uuid0}
properties
(
    "type"="adbc",
    "adbc.driver"="flight_sql",
    "adbc.url"="grpc://${external_flightsql_ip}:${external_flightsql_port}"
);
-- result:
-- !result
set catalog adbc_catalog_${uuid0};
-- result:
-- !result
[UC]explain select name from test_db.test_types where id > 3;
-- result:
-- !result
set catalog default_catalog;
-- result:
-- !result
drop catalog adbc_catalog_${uuid0};
-- result:
-- !result

-- name: test_adbc_subquery
set catalog default_catalog;
-- result:
-- !result
create external catalog adbc_catalog_${uuid0}
properties
(
    "type"="adbc",
    "adbc.driver"="flight_sql",
    "adbc.url"="grpc://${external_flightsql_ip}:${external_flightsql_port}"
);
-- result:
-- !result
set catalog adbc_catalog_${uuid0};
-- result:
-- !result
select name from test_db.test_types where id in (select id from test_db.test_join where category = 'A') order by name;
-- result:
alpha
gamma
-- !result
set catalog default_catalog;
-- result:
-- !result
drop catalog adbc_catalog_${uuid0};
-- result:
-- !result
