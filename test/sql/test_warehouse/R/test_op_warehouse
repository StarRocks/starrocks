-- name: __test_op_warehouse
CREATE WAREHOUSE IF NOT EXISTS aaa comment "for test";
-- result:
-- !result
SHOW WAREHOUSES;
-- result:
[REGEX]0	default_warehouse	INITIALIZING	1	An internal warehouse contains all compute nodes in this system
.*	aaa	INITIALIZING	1	for test
-- !result
SHOW PROC '/warehouses/aaa';
-- result:
[REGEX].*	.*	\[.*\]	-1	-1
-- !result
SUSPEND WAREHOUSE aaa;
-- result:
-- !result
SHOW PROC '/warehouses/aaa';
-- result:
[REGEX].*	.*	\[.*\]	-1	-1
-- !result
RESUME WAREHOUSE aaa;
-- result:
-- !result
SHOW PROC '/warehouses/aaa';
-- result:
[REGEX].*	.*	\[.*\]	-1	-1
-- !result
DROP WAREHOUSE aaa;
-- result:
-- !result
SHOW WAREHOUSES;
-- result:
0	default_warehouse	INITIALIZING	1	An internal warehouse contains all compute nodes in this system
-- !result
