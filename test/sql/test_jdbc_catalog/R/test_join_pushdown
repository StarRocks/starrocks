-- name: test_jdbc_catalog_join_pushdown
shell: mysql -h${external_mysql_ip} -u${external_mysql_user} -p${external_mysql_password} -P${external_mysql_port} -e 'drop database if exists db_join_pushdown_${uuid0}; create database db_join_pushdown_${uuid0};'
-- result:
0

-- !result
shell: mysql -h${external_mysql_ip} -u${external_mysql_user} -p${external_mysql_password} -P${external_mysql_port} -e 'use db_join_pushdown_${uuid0}; CREATE TABLE orders (order_id int not null, customer_id int not null, amount int not null, status varchar(20));'
-- result:
0

-- !result
shell: mysql -h${external_mysql_ip} -u${external_mysql_user} -p${external_mysql_password} -P${external_mysql_port} -e 'use db_join_pushdown_${uuid0}; CREATE TABLE customers (customer_id int not null, region varchar(20));'
-- result:
0

-- !result
shell: mysql -h${external_mysql_ip} -u${external_mysql_user} -p${external_mysql_password} -P${external_mysql_port} -e 'use db_join_pushdown_${uuid0}; CREATE TABLE regions (customer_id int not null, region_group varchar(20));'
-- result:
0

-- !result
shell: mysql -h${external_mysql_ip} -u${external_mysql_user} -p${external_mysql_password} -P${external_mysql_port} -e 'use db_join_pushdown_${uuid0}; INSERT INTO orders VALUES (1,10,15,"open"),(2,10,25,"open"),(3,20,30,"paid"),(4,30,45,"open"),(5,40,55,"paid"),(6,50,18,NULL);'
-- result:
0

-- !result
shell: mysql -h${external_mysql_ip} -u${external_mysql_user} -p${external_mysql_password} -P${external_mysql_port} -e 'use db_join_pushdown_${uuid0}; INSERT INTO customers VALUES (10,"east"),(20,"west"),(30,"north"),(50,"east");'
-- result:
0

-- !result
shell: mysql -h${external_mysql_ip} -u${external_mysql_user} -p${external_mysql_password} -P${external_mysql_port} -e 'use db_join_pushdown_${uuid0}; INSERT INTO regions VALUES (10,"coastal"),(20,"inland"),(30,"inland");'
-- result:
0

-- !result
set catalog default_catalog;
-- result:
-- !result
create external catalog jdbc_join_pushdown_${uuid0}
properties
(
    "type"="jdbc",
    "user"="${external_mysql_user}",
    "password"="${external_mysql_password}",
    "jdbc_uri"="jdbc:mysql://${external_mysql_ip}:${external_mysql_port}",
    "driver_url"="${jdbc_url}",
    "driver_class"="com.mysql.cj.jdbc.Driver"
);
-- result:
-- !result
create external catalog jdbc_join_pushdown_alt_${uuid0}
properties
(
    "type"="jdbc",
    "user"="${external_mysql_user}",
    "password"="${external_mysql_password}",
    "jdbc_uri"="jdbc:mysql://${external_mysql_ip}:${external_mysql_port}",
    "driver_url"="${jdbc_url}",
    "driver_class"="com.mysql.cj.jdbc.Driver"
);
-- result:
-- !result
create database db_join_pushdown_internal_${uuid0};
-- result:
-- !result
create table db_join_pushdown_internal_${uuid0}.vip_customers (
    customer_id int,
    label varchar(20)
)
duplicate key(customer_id)
distributed by hash(customer_id) buckets 1
properties("replication_num" = "1");
-- result:
-- !result
insert into db_join_pushdown_internal_${uuid0}.vip_customers values (10, 'gold'), (30, 'silver'), (40, 'bronze');
-- result:
-- !result
explain select o.order_id, c.region, o.amount
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
where o.amount >= 20 and c.region in ('east', 'west')
order by o.order_id;
-- result:
[REGEX]PLAN FRAGMENT 0[ \t]*
 OUTPUT EXPRS:1: order_id \| 6: region \| 3: amount[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  RESULT SINK[ \t]*
[ \t]*
  3:MERGING-EXCHANGE[ \t]*
[ \t]*
PLAN FRAGMENT 1[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 03[ \t]*
    UNPARTITIONED[ \t]*
[ \t]*
  2:SORT[ \t]*
  \|  order by: <slot 1> 1: order_id ASC[ \t]*
  \|  offset: 0[ \t]*
  \|[ \t]*
  1:Project[ \t]*
  \|  <slot 1> : order_id[ \t]*
  \|  <slot 3> : amount[ \t]*
  \|  <slot 6> : region[ \t]*
  \|[ \t]*
  0:SCAN JDBC[ \t]*
     TABLE: \(SELECT sr_t0\.`order_id` AS sr_c1, sr_t0\.`amount` AS sr_c3, sr_t1\.`region` AS sr_c6 FROM \(SELECT `order_id`, `customer_id`, `amount` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`amount` >= 20\)\) sr_t0, \(SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`region` IN \('east', 'west'\)\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged[ \t]*
     QUERY: SELECT `sr_c1`, `sr_c3`, `sr_c6` FROM \(SELECT sr_t0\.`order_id` AS sr_c1, sr_t0\.`amount` AS sr_c3, sr_t1\.`region` AS sr_c6 FROM \(SELECT `order_id`, `customer_id`, `amount` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`amount` >= 20\)\) sr_t0, \(SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`region` IN \('east', 'west'\)\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged[ \t]*
-- !result
select o.order_id, c.region, o.amount
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
where o.amount >= 20 and c.region in ('east', 'west')
order by o.order_id;
-- result:
2	east	25
3	west	30
-- !result
explain select o.order_id, c.region
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
where o.amount >= 20
order by o.order_id;
-- result:
[REGEX]PLAN FRAGMENT 0[ \t]*
 OUTPUT EXPRS:1: order_id \| 6: region[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  RESULT SINK[ \t]*
[ \t]*
  3:MERGING-EXCHANGE[ \t]*
[ \t]*
PLAN FRAGMENT 1[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 03[ \t]*
    UNPARTITIONED[ \t]*
[ \t]*
  2:SORT[ \t]*
  \|  order by: <slot 1> 1: order_id ASC[ \t]*
  \|  offset: 0[ \t]*
  \|[ \t]*
  1:Project[ \t]*
  \|  <slot 1> : order_id[ \t]*
  \|  <slot 6> : region[ \t]*
  \|[ \t]*
  0:SCAN JDBC[ \t]*
     TABLE: \(SELECT sr_t0\.`order_id` AS sr_c1, sr_t1\.`region` AS sr_c6 FROM \(SELECT `order_id`, `customer_id`, `amount` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`amount` >= 20\)\) sr_t0, \(SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged[ \t]*
     QUERY: SELECT `sr_c1`, `sr_c6` FROM \(SELECT sr_t0\.`order_id` AS sr_c1, sr_t1\.`region` AS sr_c6 FROM \(SELECT `order_id`, `customer_id`, `amount` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`amount` >= 20\)\) sr_t0, \(SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged[ \t]*
-- !result
select o.order_id, c.region
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
where o.amount >= 20
order by o.order_id;
-- result:
2	east
3	west
4	north
-- !result
explain select o.order_id, r.region_group, o.status, o.amount
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.regions r
on c.customer_id = r.customer_id
where o.amount between 20 and 50
order by o.order_id;
-- result:
[REGEX]PLAN FRAGMENT 0[ \t]*
 OUTPUT EXPRS:1: order_id \| 8: region_group \| 4: status \| 3: amount[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  RESULT SINK[ \t]*
[ \t]*
  3:MERGING-EXCHANGE[ \t]*
[ \t]*
PLAN FRAGMENT 1[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 03[ \t]*
    UNPARTITIONED[ \t]*
[ \t]*
  2:SORT[ \t]*
  \|  order by: <slot 1> 1: order_id ASC[ \t]*
  \|  offset: 0[ \t]*
  \|[ \t]*
  1:Project[ \t]*
  \|  <slot 1> : order_id[ \t]*
  \|  <slot 3> : amount[ \t]*
  \|  <slot 4> : status[ \t]*
  \|  <slot 8> : region_group[ \t]*
  \|[ \t]*
  0:SCAN JDBC[ \t]*
     TABLE: \(SELECT sr_t0\.`order_id` AS sr_c1, sr_t0\.`amount` AS sr_c3, sr_t0\.`status` AS sr_c4, sr_t2\.`region_group` AS sr_c8 FROM \(SELECT `order_id`, `customer_id`, `amount`, `status` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`amount` >= 20\) AND \(`amount` <= 50\)\) sr_t0, \(SELECT `customer_id` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1, \(SELECT `customer_id`, `region_group` FROM `db_join_pushdown_[0-9a-f]+`\.`regions` WHERE \(`customer_id` IS NOT NULL\)\) sr_t2 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\) AND \(sr_t1\.`customer_id` = sr_t2\.`customer_id`\)\) sr_merged[ \t]*
     QUERY: SELECT `sr_c1`, `sr_c3`, `sr_c4`, `sr_c8` FROM \(SELECT sr_t0\.`order_id` AS sr_c1, sr_t0\.`amount` AS sr_c3, sr_t0\.`status` AS sr_c4, sr_t2\.`region_group` AS sr_c8 FROM \(SELECT `order_id`, `customer_id`, `amount`, `status` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`amount` >= 20\) AND \(`amount` <= 50\)\) sr_t0, \(SELECT `customer_id` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1, \(SELECT `customer_id`, `region_group` FROM `db_join_pushdown_[0-9a-f]+`\.`regions` WHERE \(`customer_id` IS NOT NULL\)\) sr_t2 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\) AND \(sr_t1\.`customer_id` = sr_t2\.`customer_id`\)\) sr_merged[ \t]*
-- !result
select o.order_id, r.region_group, o.status, o.amount
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.regions r
on c.customer_id = r.customer_id
where o.amount between 20 and 50
order by o.order_id;
-- result:
2	coastal	open	25
3	inland	paid	30
4	inland	open	45
-- !result
explain select o.order_id, c.region, coalesce(o.status, 'missing')
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
where o.status is null
order by o.order_id;
-- result:
[REGEX]PLAN FRAGMENT 0[ \t]*
 OUTPUT EXPRS:1: order_id \| 6: region \| 7: coalesce[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  RESULT SINK[ \t]*
[ \t]*
  3:MERGING-EXCHANGE[ \t]*
[ \t]*
PLAN FRAGMENT 1[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 03[ \t]*
    UNPARTITIONED[ \t]*
[ \t]*
  2:SORT[ \t]*
  \|  order by: <slot 1> 1: order_id ASC[ \t]*
  \|  offset: 0[ \t]*
  \|[ \t]*
  1:Project[ \t]*
  \|  <slot 1> : order_id[ \t]*
  \|  <slot 6> : region[ \t]*
  \|  <slot 7> : coalesce\(status, 'missing'\)[ \t]*
  \|[ \t]*
  0:SCAN JDBC[ \t]*
     TABLE: \(SELECT sr_t0\.`order_id` AS sr_c1, sr_t0\.`status` AS sr_c4, sr_t1\.`region` AS sr_c6 FROM \(SELECT `order_id`, `customer_id`, `status` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`status` IS NULL\)\) sr_t0, \(SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged[ \t]*
     QUERY: SELECT `sr_c1`, `sr_c4`, `sr_c6` FROM \(SELECT sr_t0\.`order_id` AS sr_c1, sr_t0\.`status` AS sr_c4, sr_t1\.`region` AS sr_c6 FROM \(SELECT `order_id`, `customer_id`, `status` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`status` IS NULL\)\) sr_t0, \(SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged[ \t]*
-- !result
select o.order_id, c.region, coalesce(o.status, 'missing')
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
where o.status is null
order by o.order_id;
-- result:
6	east	missing
-- !result
explain select o.order_id, c.customer_id
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on abs(o.customer_id - 20) = c.customer_id
where o.order_id in (1, 2)
order by o.order_id, c.customer_id;
-- result:
[REGEX]PLAN FRAGMENT 0[ \t]*
 OUTPUT EXPRS:1: order_id \| 5: customer_id[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  RESULT SINK[ \t]*
[ \t]*
  9:MERGING-EXCHANGE[ \t]*
[ \t]*
PLAN FRAGMENT 1[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: HASH_PARTITIONED: 7: abs[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 09[ \t]*
    UNPARTITIONED[ \t]*
[ \t]*
  8:SORT[ \t]*
  \|  order by: <slot 1> 1: order_id ASC, <slot 5> 5: customer_id ASC[ \t]*
  \|  offset: 0[ \t]*
  \|[ \t]*
  7:Project[ \t]*
  \|  <slot 1> : 1: order_id[ \t]*
  \|  <slot 5> : 5: customer_id[ \t]*
  \|[ \t]*
  6:HASH JOIN[ \t]*
  \|  join op: INNER JOIN \(PARTITIONED\)[ \t]*
  \|  colocate: false, reason:[ \t]*
  \|  equal join conjunct: 7: abs = 8: cast[ \t]*
  \|[ \t]*
  \|----5:EXCHANGE[ \t]*
  \|[ \t]*
  2:EXCHANGE[ \t]*
[ \t]*
PLAN FRAGMENT 2[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 05[ \t]*
    HASH_PARTITIONED: 8: cast[ \t]*
[ \t]*
  4:Project[ \t]*
  \|  <slot 5> : customer_id[ \t]*
  \|  <slot 8> : CAST\(5: customer_id AS LARGEINT\)[ \t]*
  \|[ \t]*
  3:SCAN JDBC[ \t]*
     TABLE: `db_join_pushdown_[0-9a-f]+`\.`customers`[ \t]*
     QUERY: SELECT `customer_id` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)[ \t]*
[ \t]*
PLAN FRAGMENT 3[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 02[ \t]*
    HASH_PARTITIONED: 7: abs[ \t]*
[ \t]*
  1:Project[ \t]*
  \|  <slot 1> : order_id[ \t]*
  \|  <slot 7> : abs\(CAST\(customer_id AS BIGINT\) - 20\)[ \t]*
  \|[ \t]*
  0:SCAN JDBC[ \t]*
     TABLE: `db_join_pushdown_[0-9a-f]+`\.`orders`[ \t]*
     QUERY: SELECT `order_id`, `customer_id` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`order_id` IN \(1, 2\)\)[ \t]*
-- !result
select o.order_id, c.customer_id
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on abs(o.customer_id - 20) = c.customer_id
where o.order_id in (1, 2)
order by o.order_id, c.customer_id;
-- result:
1	10
2	10
-- !result
explain select o.order_id, c.region, o.amount
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
left join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
where o.amount >= 45
order by o.order_id;
-- result:
[REGEX]PLAN FRAGMENT 0[ \t]*
 OUTPUT EXPRS:1: order_id \| 6: region \| 3: amount[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  RESULT SINK[ \t]*
[ \t]*
  7:MERGING-EXCHANGE[ \t]*
[ \t]*
PLAN FRAGMENT 1[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: HASH_PARTITIONED: customer_id[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 07[ \t]*
    UNPARTITIONED[ \t]*
[ \t]*
  6:SORT[ \t]*
  \|  order by: <slot 1> 1: order_id ASC[ \t]*
  \|  offset: 0[ \t]*
  \|[ \t]*
  5:Project[ \t]*
  \|  <slot 1> : order_id[ \t]*
  \|  <slot 3> : amount[ \t]*
  \|  <slot 6> : region[ \t]*
  \|[ \t]*
  4:HASH JOIN[ \t]*
  \|  join op: RIGHT OUTER JOIN \(PARTITIONED\)[ \t]*
  \|  colocate: false, reason:[ \t]*
  \|  equal join conjunct: customer_id = customer_id[ \t]*
  \|[ \t]*
  \|----3:EXCHANGE[ \t]*
  \|[ \t]*
  1:EXCHANGE[ \t]*
[ \t]*
PLAN FRAGMENT 2[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 03[ \t]*
    HASH_PARTITIONED: customer_id[ \t]*
[ \t]*
  2:SCAN JDBC[ \t]*
     TABLE: `db_join_pushdown_[0-9a-f]+`\.`orders`[ \t]*
     QUERY: SELECT `order_id`, `customer_id`, `amount` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`amount` >= 45\)[ \t]*
[ \t]*
PLAN FRAGMENT 3[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 01[ \t]*
    HASH_PARTITIONED: customer_id[ \t]*
[ \t]*
  0:SCAN JDBC[ \t]*
     TABLE: `db_join_pushdown_[0-9a-f]+`\.`customers`[ \t]*
     QUERY: SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers`[ \t]*
-- !result
select o.order_id, c.region, o.amount
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
left join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
where o.amount >= 45
order by o.order_id;
-- result:
4	north	45
5	None	55
-- !result
explain select o.order_id, c.region, v.label, o.amount
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
join default_catalog.db_join_pushdown_internal_${uuid0}.vip_customers v
on o.customer_id = v.customer_id
where o.amount >= 20
order by o.order_id;
-- result:
[REGEX]PLAN FRAGMENT 0[ \t]*
 OUTPUT EXPRS:1: order_id \| 6: region \| 8: label \| 3: amount[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  RESULT SINK[ \t]*
[ \t]*
  6:MERGING-EXCHANGE[ \t]*
[ \t]*
PLAN FRAGMENT 1[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: RANDOM[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 06[ \t]*
    UNPARTITIONED[ \t]*
[ \t]*
  5:SORT[ \t]*
  \|  order by: <slot 1> 1: order_id ASC[ \t]*
  \|  offset: 0[ \t]*
  \|[ \t]*
  4:Project[ \t]*
  \|  <slot 1> : order_id[ \t]*
  \|  <slot 3> : amount[ \t]*
  \|  <slot 6> : region[ \t]*
  \|  <slot 8> : 8: label[ \t]*
  \|[ \t]*
  3:HASH JOIN[ \t]*
  \|  join op: INNER JOIN \(BUCKET_SHUFFLE\)[ \t]*
  \|  colocate: false, reason:[ \t]*
  \|  equal join conjunct: 7: customer_id = customer_id[ \t]*
  \|[ \t]*
  \|----2:EXCHANGE[ \t]*
  \|[ \t]*
  0:OlapScanNode[ \t]*
     TABLE: vip_customers[ \t]*
     PREAGGREGATION: ON[ \t]*
     PREDICATES: 7: customer_id IS NOT NULL[ \t]*
     partitions=1/1[ \t]*
     rollup: vip_customers[ \t]*
     tabletRatio=1/1[ \t]*
     tabletList=[0-9]+[ \t]*
     cardinality=3[ \t]*
     avgRowSize=9\.333333[ \t]*
[ \t]*
PLAN FRAGMENT 2[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 02[ \t]*
    BUCKET_SHUFFLE_HASH_PARTITIONED: customer_id[ \t]*
[ \t]*
  1:SCAN JDBC[ \t]*
     TABLE: \(SELECT sr_t0\.`order_id` AS sr_c1, sr_t0\.`customer_id` AS sr_c2, sr_t0\.`amount` AS sr_c3, sr_t1\.`region` AS sr_c6 FROM \(SELECT `order_id`, `customer_id`, `amount` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`amount` >= 20\)\) sr_t0, \(SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged[ \t]*
     QUERY: SELECT `sr_c1`, `sr_c2`, `sr_c3`, `sr_c6` FROM \(SELECT sr_t0\.`order_id` AS sr_c1, sr_t0\.`customer_id` AS sr_c2, sr_t0\.`amount` AS sr_c3, sr_t1\.`region` AS sr_c6 FROM \(SELECT `order_id`, `customer_id`, `amount` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`amount` >= 20\)\) sr_t0, \(SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged[ \t]*
-- !result
select o.order_id, c.region, v.label, o.amount
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
join default_catalog.db_join_pushdown_internal_${uuid0}.vip_customers v
on o.customer_id = v.customer_id
where o.amount >= 20
order by o.order_id;
-- result:
2	east	gold	25
4	north	silver	45
-- !result
explain select o.order_id, c.region, o.amount
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_alt_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
where o.order_id in (2, 3, 4)
order by o.order_id;
-- result:
[REGEX]PLAN FRAGMENT 0[ \t]*
 OUTPUT EXPRS:1: order_id \| 6: region \| 3: amount[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  RESULT SINK[ \t]*
[ \t]*
  7:MERGING-EXCHANGE[ \t]*
[ \t]*
PLAN FRAGMENT 1[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: HASH_PARTITIONED: customer_id[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 07[ \t]*
    UNPARTITIONED[ \t]*
[ \t]*
  6:SORT[ \t]*
  \|  order by: <slot 1> 1: order_id ASC[ \t]*
  \|  offset: 0[ \t]*
  \|[ \t]*
  5:Project[ \t]*
  \|  <slot 1> : order_id[ \t]*
  \|  <slot 3> : amount[ \t]*
  \|  <slot 6> : region[ \t]*
  \|[ \t]*
  4:HASH JOIN[ \t]*
  \|  join op: INNER JOIN \(PARTITIONED\)[ \t]*
  \|  colocate: false, reason:[ \t]*
  \|  equal join conjunct: customer_id = customer_id[ \t]*
  \|[ \t]*
  \|----3:EXCHANGE[ \t]*
  \|[ \t]*
  1:EXCHANGE[ \t]*
[ \t]*
PLAN FRAGMENT 2[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 03[ \t]*
    HASH_PARTITIONED: customer_id[ \t]*
[ \t]*
  2:SCAN JDBC[ \t]*
     TABLE: `db_join_pushdown_[0-9a-f]+`\.`orders`[ \t]*
     QUERY: SELECT `order_id`, `customer_id`, `amount` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`order_id` IN \(2, 3, 4\)\)[ \t]*
[ \t]*
PLAN FRAGMENT 3[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 01[ \t]*
    HASH_PARTITIONED: customer_id[ \t]*
[ \t]*
  0:SCAN JDBC[ \t]*
     TABLE: `db_join_pushdown_[0-9a-f]+`\.`customers`[ \t]*
     QUERY: SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)[ \t]*
-- !result
select o.order_id, c.region, o.amount
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_alt_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
where o.order_id in (2, 3, 4)
order by o.order_id;
-- result:
2	east	25
3	west	30
4	north	45
-- !result
explain select o.order_id, c.region, ao.order_id, ac.region
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
join jdbc_join_pushdown_alt_${uuid0}.db_join_pushdown_${uuid0}.orders ao
join jdbc_join_pushdown_alt_${uuid0}.db_join_pushdown_${uuid0}.customers ac
on ao.customer_id = ac.customer_id
where o.order_id = 2 and ao.order_id in (3, 4)
order by o.order_id, ao.order_id;
-- result:
[REGEX]PLAN FRAGMENT 0[ \t]*
 OUTPUT EXPRS:1: order_id \| 6: region \| 7: order_id \| 12: region[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  RESULT SINK[ \t]*
[ \t]*
  6:MERGING-EXCHANGE[ \t]*
[ \t]*
PLAN FRAGMENT 1[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 06[ \t]*
    UNPARTITIONED[ \t]*
[ \t]*
  5:SORT[ \t]*
  \|  order by: <slot 1> 1: order_id ASC, <slot 7> 7: order_id ASC[ \t]*
  \|  offset: 0[ \t]*
  \|[ \t]*
  4:Project[ \t]*
  \|  <slot 1> : order_id[ \t]*
  \|  <slot 6> : region[ \t]*
  \|  <slot 7> : order_id[ \t]*
  \|  <slot 12> : region[ \t]*
  \|[ \t]*
  3:NESTLOOP JOIN[ \t]*
  \|  join op: CROSS JOIN[ \t]*
  \|  colocate: false, reason:[ \t]*
  \|[ \t]*
  \|----2:EXCHANGE[ \t]*
  \|[ \t]*
  0:SCAN JDBC[ \t]*
     TABLE: \(SELECT sr_t0\.`order_id` AS sr_c1, sr_t1\.`region` AS sr_c6 FROM \(SELECT `order_id`, `customer_id` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`order_id` = 2\)\) sr_t0, \(SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged[ \t]*
     QUERY: SELECT `sr_c1`, `sr_c6` FROM \(SELECT sr_t0\.`order_id` AS sr_c1, sr_t1\.`region` AS sr_c6 FROM \(SELECT `order_id`, `customer_id` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`order_id` = 2\)\) sr_t0, \(SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged[ \t]*
[ \t]*
PLAN FRAGMENT 2[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 02[ \t]*
    UNPARTITIONED[ \t]*
[ \t]*
  1:SCAN JDBC[ \t]*
     TABLE: \(SELECT sr_t0\.`order_id` AS sr_c7, sr_t1\.`region` AS sr_c12 FROM \(SELECT `order_id`, `customer_id` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`order_id` IN \(3, 4\)\)\) sr_t0, \(SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged[ \t]*
     QUERY: SELECT `sr_c7`, `sr_c12` FROM \(SELECT sr_t0\.`order_id` AS sr_c7, sr_t1\.`region` AS sr_c12 FROM \(SELECT `order_id`, `customer_id` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`order_id` IN \(3, 4\)\)\) sr_t0, \(SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged[ \t]*
-- !result
select o.order_id, c.region, ao.order_id, ac.region
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
join jdbc_join_pushdown_alt_${uuid0}.db_join_pushdown_${uuid0}.orders ao
join jdbc_join_pushdown_alt_${uuid0}.db_join_pushdown_${uuid0}.customers ac
on ao.customer_id = ac.customer_id
where o.order_id = 2 and ao.order_id in (3, 4)
order by o.order_id, ao.order_id;
-- result:
2	east	3	west
2	east	4	north
-- !result
explain select o.order_id, c.region
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
limit 100;
-- result:
[REGEX]PLAN FRAGMENT 0[ \t]*
 OUTPUT EXPRS:1: order_id \| 6: region[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  RESULT SINK[ \t]*
[ \t]*
  1:Project[ \t]*
  \|  <slot 1> : order_id[ \t]*
  \|  <slot 6> : region[ \t]*
  \|  limit: 100[ \t]*
  \|[ \t]*
  0:SCAN JDBC[ \t]*
     TABLE: \(SELECT sr_t0\.`order_id` AS sr_c1, sr_t1\.`region` AS sr_c6 FROM \(SELECT `order_id`, `customer_id` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`customer_id` IS NOT NULL\)\) sr_t0, \(SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged[ \t]*
     QUERY: SELECT `sr_c1`, `sr_c6` FROM \(SELECT sr_t0\.`order_id` AS sr_c1, sr_t1\.`region` AS sr_c6 FROM \(SELECT `order_id`, `customer_id` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`customer_id` IS NOT NULL\)\) sr_t0, \(SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged LIMIT 100[ \t]*
     limit: 100[ \t]*
-- !result
select count(*) from (
    select o.order_id, c.region
    from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
    join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
    on o.customer_id = c.customer_id
    limit 100
) t;
-- result:
5
-- !result
explain select count(*)
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id;
-- result:
[REGEX]PLAN FRAGMENT 0[ \t]*
 OUTPUT EXPRS:count[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  RESULT SINK[ \t]*
[ \t]*
  0:SCAN JDBC[ \t]*
     TABLE: \(SELECT count\(\*\) AS `jdbc_agg_7` FROM \(SELECT 1 AS `jdbc_proj_9` FROM \(SELECT sr_t0\.`customer_id` AS sr_c2 FROM \(SELECT `customer_id` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`customer_id` IS NOT NULL\)\) sr_t0, \(SELECT `customer_id` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged\) sr_merged\) sr_merged[ \t]*
     QUERY: SELECT `jdbc_agg_7` FROM \(SELECT count\(\*\) AS `jdbc_agg_7` FROM \(SELECT 1 AS `jdbc_proj_9` FROM \(SELECT sr_t0\.`customer_id` AS sr_c2 FROM \(SELECT `customer_id` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`customer_id` IS NOT NULL\)\) sr_t0, \(SELECT `customer_id` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`customer_id` IS NOT NULL\)\) sr_t1 WHERE \(sr_t0\.`customer_id` = sr_t1\.`customer_id`\)\) sr_merged\) sr_merged\) sr_merged[ \t]*
-- !result
select count(*)
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id;
-- result:
5
-- !result
set enable_jdbc_join_push_down=false;
-- result:
-- !result
explain select o.order_id, c.region, o.amount
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
where o.amount >= 20 and c.region in ('east', 'west')
order by o.order_id;
-- result:
[REGEX]PLAN FRAGMENT 0[ \t]*
 OUTPUT EXPRS:1: order_id \| 6: region \| 3: amount[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  RESULT SINK[ \t]*
[ \t]*
  7:MERGING-EXCHANGE[ \t]*
[ \t]*
PLAN FRAGMENT 1[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: HASH_PARTITIONED: customer_id[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 07[ \t]*
    UNPARTITIONED[ \t]*
[ \t]*
  6:SORT[ \t]*
  \|  order by: <slot 1> 1: order_id ASC[ \t]*
  \|  offset: 0[ \t]*
  \|[ \t]*
  5:Project[ \t]*
  \|  <slot 1> : order_id[ \t]*
  \|  <slot 3> : amount[ \t]*
  \|  <slot 6> : region[ \t]*
  \|[ \t]*
  4:HASH JOIN[ \t]*
  \|  join op: INNER JOIN \(PARTITIONED\)[ \t]*
  \|  colocate: false, reason:[ \t]*
  \|  equal join conjunct: customer_id = customer_id[ \t]*
  \|[ \t]*
  \|----3:EXCHANGE[ \t]*
  \|[ \t]*
  1:EXCHANGE[ \t]*
[ \t]*
PLAN FRAGMENT 2[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 03[ \t]*
    HASH_PARTITIONED: customer_id[ \t]*
[ \t]*
  2:SCAN JDBC[ \t]*
     TABLE: `db_join_pushdown_[0-9a-f]+`\.`orders`[ \t]*
     QUERY: SELECT `order_id`, `customer_id`, `amount` FROM `db_join_pushdown_[0-9a-f]+`\.`orders` WHERE \(`amount` >= 20\)[ \t]*
[ \t]*
PLAN FRAGMENT 3[ \t]*
 OUTPUT EXPRS:[ \t]*
  PARTITION: UNPARTITIONED[ \t]*
[ \t]*
  STREAM DATA SINK[ \t]*
    EXCHANGE ID: 01[ \t]*
    HASH_PARTITIONED: customer_id[ \t]*
[ \t]*
  0:SCAN JDBC[ \t]*
     TABLE: `db_join_pushdown_[0-9a-f]+`\.`customers`[ \t]*
     QUERY: SELECT `customer_id`, `region` FROM `db_join_pushdown_[0-9a-f]+`\.`customers` WHERE \(`region` IN \('east', 'west'\)\)[ \t]*
-- !result
select o.order_id, c.region, o.amount
from jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.orders o
join jdbc_join_pushdown_${uuid0}.db_join_pushdown_${uuid0}.customers c
on o.customer_id = c.customer_id
where o.amount >= 20 and c.region in ('east', 'west')
order by o.order_id;
-- result:
2	east	25
3	west	30
-- !result
set enable_jdbc_join_push_down=true;
-- result:
-- !result
drop catalog jdbc_join_pushdown_${uuid0};
-- result:
-- !result
drop catalog jdbc_join_pushdown_alt_${uuid0};
-- result:
-- !result
drop database db_join_pushdown_internal_${uuid0} force;
-- result:
-- !result
shell: mysql -h${external_mysql_ip} -u${external_mysql_user} -p${external_mysql_password} -P${external_mysql_port} -e 'drop database db_join_pushdown_${uuid0};'
-- result:
0

-- !result