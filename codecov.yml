comment:
  layout: "newheader, diff, flags, files, newfooter"
coverage:
  status:
    project:
      fe-ut:
        target: 60%
        threshold: 5%
        only_pulls: true
    patch:
      fe-ut:
        target: 80%
        only_pulls: true