#!/bin/sh
set -e

case "$1" in
    remove|deconfigure)
        for COMP in fe be cn; do
            if systemctl is-active --quiet starrocks-$COMP; then
                echo "Stopping starrocks-$COMP service..."
                systemctl stop starrocks-$COMP || true
            fi
        done
    ;;
    *) ;;
esac
exit 0